# Roadmap — AI Knowledge Companion

**Status:** Phases 1–5 done; Phase 6 feature-complete with **§6.1 semantic search (P6-A) shipped; §6.2 global knowledge graph (P6-B + P6-C) shipped; §6.3 plugin system (P6-D) shipped; §6.4 universal input shipped — manual notes (P6-E), images/PDFs via OCR (P6-F), email/document parsers (P6-G), screenshots (P6-H follow-up), multi-input sessions deferred ("if validated by usage"); §6.5 specialized testing shipped (P6-H)**. Flutter app in `src/`: **339 unit/widget tests green + 3 skipped, `flutter analyze` clean**. Engine: **317 pytest green, ruff clean**. Android app in `app/`: **Jetpack Compose + Room v3 + Gemini Thinking Mode + Document OCR compile clean**.

## How to use & update this file

- Each phase has a **Definition of Done** — only check that phase `Done` when every item is satisfied.
- After every milestone, update this file: tick completed tasks, move the phase status, append a row to the **Milestone log** at the bottom. The project convention is: **Roadmap.md is updated in the same change that completes a milestone.**
- A task is `[x]` only when merged and verified (tests pass, not merely implemented).
- If a task needs to change (scope, order, drop), update it here in the same change that changes the work — never let the roadmap and reality diverge.

---

## Overall status

| Phase | Name | Status | Est. scope |
|---|---|---|---|
| 1 | Foundation (MVP) | 🟡 Feature-complete* | Flutter shell, auth, capture, local DB, sync, engine skeleton |
| 2 | AI Processing | 🟡 Feature-complete* | Orchestrator, stages, prompts, lifecycle, SSE |
| 3 | Interactive Editing | 🟢 Done | Op-log undo/redo, version history, diff sync, transcript editing + re-run (P3-A–P3-F) |
| 4 | Knowledge Management | 🟢 Done | Search, tags, org, graph browser |
| 5 | AI Productivity | 🟢 Done | Commands, chat, insights, export |
| 6 | Intelligence | 🟡 In progress | Embeddings, global graph, plugins, universal inputs (notes + OCR images/PDFs + email/docs parsers + screenshots shipped; multi-input sessions deferred), §6.5 quality testing shipped |

**Legend:** 🔴 Not started · 🟡 In progress · 🟢 Done

\* Phases 1–2 are feature-complete; the 🟡 mark reflects infra-blocked (device/credentials) items only: §1.8 E2E smoke + Fastlane, §2.5 on-device E2E.

---

# Phase 1 — Foundation (MVP)

**Deliverable (spec §28.1):** Users can log in, record audio, and configure AI providers.

**Architecture refs:** §3 (frontend), §4.1, §4.12 (engine skeleton + voice input adapter), §5.3 (drift schema v1), §6 (auth), §8.3 (mobile CI/CD), §10 (testing).

### 1.1 Repository & project scaffolding
- [x] Init Flutter app in `src/` (Android + iOS), Dart + Riverpod + go_router + drift wired.
- [x] Init Python engine project in `engine/` (FastAPI app, `uv` or poetry, config module).
- [x] Add `docs/` sources of truth + AGENTS.md already in place; add lint/format config per language (analysis_options, ruff/black).
- [x] Add CI skeleton (GitHub Actions): `flutter analyze`, `flutter test`, engine unit tests, contract job stub.
- [x] Docker: `engine` image (multi-stage), `docker-compose.yml` (engine-web, engine-worker, redis) from architecture §9.

### 1.2 Local data layer (SQLite / drift)
- [x] Drift schema v1 per architecture §5.3: `sessions`, `topics`, `items`, `tags`, `session_tags`, `jobs`, `provider_settings`, `session_versions`, `entities`, `session_entities`, `relationships`, `embeddings` (tables created now, some used later).
- [x] Migrations strategy + seed migration; drift `GeneratedDatabase` build wired into codegen. *(codegen wired, schema v3; versioned chain per upgrade step + idempotent seed — see migration tests)*
- [x] DAOs for sessions/topics/items + repository interfaces in `domain/` (architecture §3.2 dependency rule).
- [x] FTS5 index setup on session text fields (used in Phase 4; create table now).

### 1.3 Authentication
- [x] Supabase project config (dev) + `supabase_flutter` init. *(local Supabase stack wired + validated; user's self-hosted stack at `132.145.181.76:8000` validated — migration applied, integration tests green; managed project URL/keys pending)*
- [x] Google Sign-In flow (architecture §6): credential → Supabase session → `flutter_secure_storage` cache.
- [x] Auth state in Riverpod (`authController`); bootstraps app + gates sync only (capture works signed-out).
- [x] RLS policies on all cloud user tables (`user_id = auth.uid()`) + migration files committed. *(sessions/topics/items + storage bucket — the tables the app syncs today; later-phase tables arrive with their own migrations)*
- [x] Sign-out: wipe user-scoped local rows + secure storage.

### 1.4 Recording (voice input adapter #1)
- [x] `record` + `just_audio` integration: start/pause/resume/stop, duration, waveform rendering. *(start/pause/resume/stop + duration ticker + live amplitude waveform — see milestone log; `just_audio` playback lands in Phase 4 §4.3)*
- [x] Background recording support; mic permission handling. *(permission handling; background support pending)*
- [x] Session draft created locally on record start (`status: recording`); audio saved to local path.
- [x] Engine `InputSource` interface (architecture §4.12) + `VoiceInputSource` adapter in engine skeleton.

### 1.5 Provider configuration (settings)
- [x] Settings screens: STT + LLM provider config (provider, base URL, model, language, temperature, timeout).
- [x] Keys persisted via `flutter_secure_storage` (device) / server secret store (engine), per architecture §12. *(device side: secure-store seam + settings save flow; engine side: server-side per-user secret store + env fallback — see milestone log)*
- [x] `provider_settings` table wired to settings UI; validation of provider config.

### 1.6 Cloud sync (basic)
- [x] Supabase data source + mappers (JSON ↔ entities) in `data/`.
- [x] Sync engine: write-through outbox, incremental pull by `updated_at`, tombstones (architecture §4.13).
- [x] Audio upload to Supabase Storage (user-scoped bucket, presigned URLs).
- [x] Session list + detail read path: SQLite first, cloud convergence in background; offline works. *(SQLite-first read; background sync on resume + pull-to-refresh + sync status + detail refresh on sync — see §1.6 note in milestone log)*

### 1.7 Engine skeleton
- [x] FastAPI app: `/healthz`, `/readyz`, `/api/v1/jobs` (create/status/cancel), SSE stream endpoint.
- [x] Redis queue + worker entrypoint; job model + structured error envelope (architecture §4.2, §7.1).
- [x] JWT verification middleware (Supabase public keys → `user_id`); service API key path for Edge Functions.
- [x] `schemas/` JSON Schema files for canonical session + jobs; vendored Dart copy + contract test job (architecture §5.2).

### 1.8 Testing & release readiness
- [x] Unit tests: domain use cases, drift migrations, mappers. *(domain + mappers green; drift migration tests added: v1→v3 and v2→v3 upgrade + fresh-create + seed idempotency)*
- [x] Widget tests: home, recording flow, settings, auth states. *(home + sync-status/refresh + session-detail convergence tests green; recording flow/controller, settings provider save + secure-key storage, and auth sign-in/out widget tests added — full suite 61 green + 3 skipped)*
- [ ] E2E smoke (`integration_test`): signed-out record → sign-in → sync. *(blocked: no emulator/device on this headless host)*
- [ ] Fastlane (TestFlight + Play internal) + `--dart-define` env config. *(`--dart-define` env config done (AppConfig); Fastlane scaffolded (`src/ios/fastlane` + `src/android/fastlane` + `Gemfile`, `staging`/`testflight`/`internal` lanes, Android env-based release signing, `.env.example`) — **not yet verified**: requires Xcode/Play credentials + a Mac/Android SDK host, see milestone log)*

**Definition of Done:** user can sign in with Google, record/pause/stop audio offline, see it locally, configure STT/LLM providers (keys in secure storage), sync a session to Supabase, and the engine serves `/api/v1/jobs` with an auth-guarded job lifecycle. CI green.

---

# Phase 2 — AI Processing

**Deliverable (spec §28.2):** Voice recordings become organized AI-generated notes.

**Architecture refs:** §4.2 (orchestrator), §4.3 (prompt registry), §4.4 (providers), §4.5 (lifecycle), §4.7 (confidence), §5.1 (canonical JSON), §7.1 (engine API), §10 (testing).

### 2.1 Orchestration layer
- [x] `Stage` protocol + `StageContext` (architecture §4.2); ordered stage list resolved from config.
- [x] Orchestrator: run stages, persist per-stage output, resume-from-stage on failure, idempotency.
- [x] Stage implementations: cleanup, topic segmentation, topic classification, entity extraction, task extraction, knowledge extraction, validation.
- [x] Token budget handling; malformed-LLM → one schema-retry → structured failure.

### 2.2 Provider adapters
- [x] `TranscriptionProvider` + `LLMProvider` protocols + registry (architecture §4.4). *(protocols + pluggable registry done; placeholder providers default; OpenAI/Deepgram/etc. adapters pending)*
- [x] STT adapters: OpenAI Whisper (API), Deepgram, AssemblyAI; `complete_structured` fallback path.
- [x] LLM adapters: OpenAI, Anthropic, Gemini, OpenRouter; Ollama + LM Studio for local profile.
- [x] Golden fixtures per adapter (recorded provider-native → canonical) + adapter unit tests.

### 2.3 Prompt registry & versioning
- [x] `engine/prompts/<stage>.<version>.json` assets for all stages (v1 each) + `PromptRegistry` (architecture §4.3).
- [x] `prompt_versions` recorded in canonical JSON; immutable old versions.
- [x] Prompt contract tests: pinned fixtures per `(stage, version)`; version bump = reviewable diff.

### 2.4 Session lifecycle + app wiring
- [x] Lifecycle state machine (architecture §4.5): `recording → uploading → transcribing → cleaning → analyzing → validating → ready → edited → synced` + `failed/cancelled`. *(engine-side: `app/lifecycle.py` — `SessionLifecycle` enum mirroring the canonical schema, `derive_session_status(job)` projection, human-readable `stage_label`, idempotent `validate_transition` incl. resume path; app drives recording/uploading + post-ready edits via the same transition table)*
- [x] Job ↔ session status mapping; SSE progress rendering in UI (stage labels + resume/retry). *(mapping + SSE progress/typed-terminal events engine-side; app side: SSE client, per-session analysis controller, progress/retry/cancel panel — see milestone log)*
- [x] Session detail screen v1: title, summary, topics → items with type + priority + confidence hints (low-confidence markers).
- [x] Automatic title + alternative titles + one-paragraph summary. *(produced by the knowledge_extraction stage into the canonical session since P2-A; UI renders alternative titles as chips + one-paragraph summary — see P2-G)*
- [x] Original vs cleaned transcript both stored; transcript tab readable. *(cleanup-stage `cleaned_text`/`original_text` read from `job.intermediates` on success, persisted to the session; reader card with Original/Cleaned toggle — see milestone log)*
- [x] Post-stop auto-submit: a new recording is submitted for analysis automatically on stop (no manual Analyze); setup failures surface as retryable. *(RecordingController.stop() fires the per-session analysis controller; AnalysisController userId read hardened so it never throws outside its guard — see P2-I)*

### 2.5 Testing
- [x] Stage purity/order/resume tests; validation-as-backbone tests (every stage output passes schema).
- [x] Cleanup-preserves-meaning golden tests.
- [ ] E2E: record → engine mock → structured session on device; failed stage → resume. *(widget-level E2E runs hermetically in CI — `test/features/e2e_record_to_session_test.dart`: real go_router + real drift DB + FakeVoiceRecorder + FakeEngineGateway covering record → analyze (SSE) → structured session and failed-stage → Retry → resume; on-device `integration_test/app_flow_test.dart` scaffolded — **blocked**: no emulator/device on this headless host, see milestone log)*

**Definition of Done:** a recorded session flows through the full orchestrated pipeline to a validated canonical Session JSON with topics, typed items, titles, summary, confidence, and prompt versions; UI shows live stage progress and a recoverable failure path.

---

# Phase 3 — Interactive Editing

**Deliverable (spec §28.3):** Users can fully refine AI-generated knowledge.

**Architecture refs:** §3.4 (state model, op-log), §4.6 (version history), §4.13 (diff sync), §5.3 (session_versions).

### 3.1 Editing primitives
- [x] Edit topics: rename, add, delete, merge, split, drag-reorder. *(domain ops — P3-A; UI + controller wiring — P3-B)*
- [x] Edit items: add/edit/delete, move across topics, change type, set priority/confidence. *(domain ops — P3-A; UI + controller wiring — P3-B)*
- [x] Transcript editing + re-run analysis (`/jobs {kind: analyze, prompt_versions}`). *(P3-F — engine `transcript` universal-input adapter (`inputs/transcript.py`, no `input_ref`, STT skipped, full pipeline from cleanup) + `orchestrator._ingest` relaxed for `input_kind == "transcript"`; Flutter `UpdateSessionTranscript` op (undoable/versioned/diff-synced) + multiline edit dialog + `AnalysisController.analyzeTranscript` re-run button on the session-detail transcript card; `ConflictResolver` handles the new field — see milestone log)*
- [x] Edit domain use cases in `domain/` (pure, testable) with repository ops. *(`EditSession` + `EditLogRepository` seam — P3-A)*

### 3.2 Op-log, undo/redo
- [x] Operation log (`InsertItem`, `MoveTopic`, `ChangeItemType`, …) as the single mutation path (architecture §3.4). *(domain `OperationLog` — P3-A; drift persistence via `SessionOplog` schema v4 + `EditLogLocalDataSource` + `EditingController` — P3-B)*
- [x] Undo/redo stack; merged debounced batches for continuous drag/typing. *(`OperationLog` cursor + `beginBatch`/`record`/`endBatch` coalescing — P3-A; `EditingController.undo/redo` + toolbar — P3-B)*

### 3.3 Version history
- [x] Version commit points: initial AI output, edit batches, re-runs, restores (architecture §4.6). *(P3-C — V1 "AI output" on first ready-with-content open / first successful analysis; every committed edit batch → its own version with a `summarizeDiff` change reason; re-analysis that changes content → "Re-analyzed with new prompts"; every restore → "Restored from v{n}" version — see milestone log)*
- [x] Version list UI + diff view (added/removed/moved topics/items). *(P3-C — `VersionHistoryScreen` newest-first timeline + per-version `_DiffPane` rendering `diffSessions` entries with kind icons/colors — see milestone log)*
- [x] Restore flow: snapshot → new working copy → new version; invariants preserved (no orphaned nodes). *(P3-C — restore overlays the snapshot onto the live session (audio/transcript/user fields preserved), commits a "Restored from v{n}" version, and clears the edit log so undo can't cross the restore boundary — see milestone log)*

### 3.4 Diff-based sync
- [x] Outbox emits op-log diffs (not full documents) to Supabase; server applies diffs (architecture §4.13). *(P3-D — `updateSession(session, {emitDiff})` enqueues `oplog_diff` outbox records for op-log edits (full-document `upsert` reserved for non-edit paths); `SyncEngine._pushDiff` pulls the cloud session, replays the pending base + batches via `ConflictResolver`, pushes the merged result and advances the log watermark; schema v5 adds `session_oplog.base_snapshot_json` — see milestone log)*
- [x] Conflict detection: field-level LWW + flagged true conflicts for user review. *(P3-D — deterministic `ConflictResolver`: field LWW with the remote value preserved, structural ops skip missing targets + anchor-translate base positions onto the merged state, true conflicts persisted to the new `sync_conflicts` table for review — see milestone log)*
- [x] Reorder/merge/split offline then sync correctly. *(P3-D — two-device offline tests cover reorder-vs-reorder, split onto a remote that gained/moved items, merge, AddTopic position anchoring — see milestone log)*

### 3.5 Testing
- [x] Op-log undo/redo unit tests; merge/split invariants. *(P3-A — 13 tests: apply/inverse round-trips, invariants, merge/split symmetry, undo/redo walk, batch coalescing, JSON+cursor round-trip, `EditSession` use case)*
- [x] Editing controller + editing-UI widget tests over the real drift stack. *(P3-B — 6 controller tests: apply persists session+op-log, undo/redo, bit-exact confidence restore, op-log survives a restart, no-op on missing session, resetLog; 4 widget tests: toolbar undo/redo disabled until edits, edit-title cancel/save/undo, item edit clears confidence + undo restores; migration tests updated to schema v4)*
- [x] Version commit/restore integration tests (incl. restore-then-undo). *(P3-C — 8 versioning-controller tests over the real drift stack (V1 commit, empty-session skip, per-batch versions with change reasons, no-op guard, undo/redo-never-commit, restore+restore-then-undo, restart persistence, getVersion) + 2 re-run commit-point tests; 12 `diffSessions`/`summarizeDiff` domain tests; 4 version-history widget tests (newest-first, diff pane, initial-version note, restore flow) — see milestone log)*
- [x] Conflict resolver deterministic tests (two-device offline simulation). *(P3-D — 15 resolver tests in `test/domain/conflict_resolver_test.dart` (clean replay, field LWW + remote-preserved, convergent edits, structural skip/converge, split/merge/reorder/move/add anchoring) + 4 oplog watermark tests + 5 sync-engine diff-drain tests + v4→v5 migration tests — see milestone log)*

**Definition of Done:** every AI output is editable with undo/redo, every edit lands in version history with restore, and edits sync as lossless diffs with deterministic conflict handling.

---

# Phase 4 — Knowledge Management

**Deliverable (spec §28.4):** A searchable, organized personal knowledge base.

**Architecture refs:** §3.3 (features), §4.8 (graph), §5.3 (FTS), §7.2 (Realtime/PostgREST).

### 4.1 Search
- [x] Global search UI + results highlighting. *(P4-A — `/search` route + AppBar search entry; results show bm25-ranked hits with the matched terms bolded via the FTS snippet's `\x01`/`\x02` markers; tap opens the session)*
- [x] FTS5 queries over titles/alt titles/summary/transcript/items/entities. *(P4-A — schema v6: `search_content` denormalized row per session + external-content `search_fts`, maintained by triggers on `sessions`/`topics`/`items`; v5→v6 migration backfills existing sessions)*
- [x] Cloud-side FTS fallback when local index stale (architectural §5.4). *(P4-A — `FallbackSearchRepository` (local-first, cloud on miss) + `SupabaseSearchDataSource` over the `search_sessions` RPC; migration `20260808000000_search_fts.sql` validated on Postgres 16)*

### 4.2 Organization
- [x] Tags: auto (AI) + manual, color-coded; `tags`/`session_tags` wired. *(P4-B — new engine `tags` stage (8-stage pipeline: … → knowledge_extraction → tags → validation) with versioned prompt asset `tags.1.json`, emitting up to 20 `{name, confidence}` tags into the canonical session (schema now carries `tags`); the app attaches them as auto-tags on analysis success (`AnalysisController._applyAutoTags`, additive + deduped, never overrides manual tags); manual find-or-create tags (case-insensitive) with color pick on the session-detail tag chip row; drift + Supabase `tags`/`session_tags` tables, schema v7 (`pinned` column), and tag/session_tag outbox drain + pull in the sync engine)*
- [x] Favorites, archive, trash (+ restore from trash), pinned sessions. *(P4-B — `OrganizationController` (toggleFavorite/Archive/Pin, trash=soft delete + clears archive, restore, purge=hard delete); home `_FilterBar` tabs All/Favorites/Archived/Trash with per-tab empty states and pinned-first sorting; session-detail AppBar star/archive/pin actions + trash popup + restore; org flags ride the canonical session JSON through the existing outbox upsert and the cloud migration adds `pinned` + `tags`/`session_tags` with RLS)*
- [x] Session detail extras: created date, duration, word count, language, favorite/archive/delete. *(P4-B — `_MetadataRow` on the session-detail screen (date/duration/words/language with per-part icons); favorite/archive/delete(+restore) are the AppBar org actions above)*

### 4.3 Audio playback
- [x] `just_audio` playback: play/pause/seek/speed, background playback; audio persists after edits. *(P4-C — `SessionAudioPlayer` adapter (swappable, mirrors the `VoiceRecorder` pattern) with a lazy, app-lifetime `JustAudioSessionAudioPlayer` so audio keeps playing across screens/backgrounded (lock-screen media controls via `audio_service` tracked as a follow-up); app-scoped `PlaybackController` (play/toggle/pause/seek/setSpeed/stop/stopForSession, one source at a time, position/duration/playing/error streams); session-detail `_AudioCard` — play/pause toggle, position/duration readout, seek slider, 0.5–2.0× speed menu, and delete-recording (confirm dialog → removes the local file + clears `audioPath`); editing the session keeps the recording (spec §16) — see milestone log)*
- [x] "Delete audio after processing" privacy setting (spec §18, architecture §12). *(P4-C — per-user switch on the Settings screen (`privacySettingsProvider` → `AppSettingsRepository` → `AppSettingsLocalDataSource` over the existing `app_meta` key/value table, key `delete_audio_after_processing:<userId>`); when on, `AnalysisController` deletes the raw recording after a session reaches `ready` via the `DeleteSessionAudio` use case (best-effort, never fails analysis); fixed a real drift bug where `updateSession`'s `write(row)` ignored null columns so clearing `audioPath`/`lastError` never persisted — now a full companion write — see milestone log)*

### 4.4 Knowledge graph browser
- [x] Per-session subgraph view (nodes/edges from `entities`/`relationships`).
- [x] Node/edge editing: add/rename/merge nodes, create/delete/relabel edges; graph edits are versions.
- [x] Graph traversal via drift locally; recursive CTE query on cloud.

### 4.5 Testing
- [x] FTS ranking tests; tag/archive/trash flows (widget + integration). *(P4-A — 17 search tests: `toFtsQuery` neutralization, index maintenance over sessions/topics/items, reindex-on-update, hard/soft delete exclusion, bm25 ranking, DAO→domain mapping, fallback local-first/miss/cloud-down, search UI debounce + highlight + navigation. P4-B — 18 org/tag/sync tests: `OrganizationController` favorite/archive/trash/restore/pin + tag toggle/pick/delete states, `TagsController` load/create/auto-attach/stats, sync engine session-tag/tag outbox drain + cascade delete + wholesale pull, migration v7 `pinned` column on all upgrade paths, home filter-tab empty states + pinned-first sort widget tests, session-detail org actions/metadata row/tag chips widget test)*
- [x] Graph identity/alias resolution + no-dangling-edges tests. *(P4-D — graph_ids determinism vs engine uuid5, graph controller add/dedupe/merge-rewire/no-self-loop/delete-cascade/edge-CRUD/undo-redo, graph screen widget tests, drift replaceSubgraph prune tests, and the Postgres `graph_traverse` RPC validated against a real Postgres 16 — see milestone log)*
- [x] Playback + privacy-setting coverage. *(P4-C — 23 new tests: playback controller (load/resume/reload, seek/speed, failure + error-stream states, stop/stopForSession/dispose), privacy controller (per-user default/toggle/load, signed-out local scope), session-detail audio card widget tests (play→pause + duration, speed menu, delete-recording with real temp file removal, audio persists after edits), analysis auto-delete (kept by default, removed when enabled), settings privacy toggle widget test, `DeleteSessionAudio` unit test. On-device playback E2E remains blocked: no emulator/device on this host — see milestone log)*

**Definition of Done:** users can search everything, organize with tags/favorites/archive/trash, play back audio, and browse/edit the per-session knowledge graph.

---

# Phase 5 — AI Productivity

**Deliverable (spec §28.5):** Each recording becomes an interactive, reusable knowledge asset.

**Architecture refs:** §4.9 (memory/insights), §4.10 (command bus), §7.1 (API), §4.11 (drafts→export seam).

### 5.1 AI command bus
- [x] Command handlers over extraction/rewrite primitives: create_tasks, create_email, create_prd, create_roadmap, create_report, create_jira_stories, create_meeting_minutes, explain, expand, rewrite, summarize (architecture §4.10).
- [x] Command prompt assets in registry; command palette UI in every session; results open as editable **Drafts** (never auto-applied).
- [x] Draft save/export path; drafts can become session content.

### 5.2 Session chat
- [x] `/chat` endpoint; grounded answers over canonical JSON (topics/items/transcript).
- [x] Chat UI per session; question examples ("What decisions did I make?", "What tasks are still open?").

### 5.3 Cross-session intelligence
- [x] `/insights` endpoint: graph clustering + vector similarity → "You've discussed Benchmark Platform in N sessions" with source links (architecture §4.9). *(P5-C — deterministic, provider-free clustering for now: the engine clusters shipped session descriptors over exact entity/tag label overlap and emits templated statements; vector similarity is Phase 6 §6.1 embeddings. See milestone log.)*
- [x] Opt-in toggle + provenance display (insights cite source sessions). *(P5-C — off by default; Settings "Enable cross-session insights" toggle + home AppBar insights entry + per-insight source chips navigating to the source sessions.)*

### 5.4 AI memory (opt-in)
- [x] Memory context injection into analysis/chat (related sessions' titles/summaries/open tasks, token-budgeted, source-tagged).
- [x] Privacy: per-user opt-in, per-session skip, revocable; no cross-user data (architecture §4.9, §12).

### 5.5 Export pipeline
- [x] Canonical JSON → Markdown/PDF/Word/TXT/JSON; clipboard/share/email.
- [x] Export respects "Markdown is export-only, never source of truth" (architecture §1.2).

### 5.6 Testing
- [x] Command draft tests (never auto-applied invariant); memory provenance tests (every answer cites sources). *(P5-F — tests only, no new user-facing feature: new `command_bus_controller_test.dart` proves a succeeded command job persists an editable draft and **never mutates the session**, failed/cancelled jobs produce no draft, and a draft enters the session only through the explicit user save path (`buildSaveDraftOperations` + editing op-log, the exact Draft-screen flow); chat memory-provenance group proves every memory-backed answer stays traceable — the chat job ships source-tagged descriptors (`session_id`/title/summary/open_tasks), `ChatState.memorySources` surfaces what rode in, the persisted answer keeps its `[memory: "…"]` citations, and memory is empty when disabled or opted out; `memory_context_test.dart` gains the invariant that every descriptor in a built memory block carries a non-empty source `session_id`; engine-side, `test_memory.py` pins the chat prompt contract that *generates* the answers, asserting it mandates inline citations (`citing sources inline like … [memory: "Title"]`, `never hallucinate`). See milestone log.)*
- [x] Insight explainability tests; chat grounding tests. *(P5-B — chat grounding tests: citation/confidence rendering + source chips in `chat_sheet_test.dart`. P5-C — insight tests: clustering determinism, confidence curve, provenance snippets, API surfaces in `engine/tests/test_insights.py`; Flutter `insight_test.dart` + `insights_controller_test.dart` + `insights_screen_test.dart`.)*

**Definition of Done:** every session is chat-enabled, has a reusable command palette producing editable drafts, exports cleanly, and — when opted in — surfaces memory-aware answers and cross-session insights with full provenance.

---

# Phase 6 — Intelligence

**Deliverable (spec §28.6):** The app evolves into a true "second brain."

**Architecture refs:** §4.8 (global graph), §4.11 (plugins), §4.12 (universal inputs), §5.3–5.4 (embeddings), §13.

### 6.1 Semantic search & related sessions
- [x] Embeddings pipeline (engine) + local on-device / `pgvector` cloud (architecture §5.3, §5.4). *(P6-A — engine: 8-stage pipeline gains an `embedding` stage (stage-8, all-MiniLM-L6-v2 via sentence-transformers) + versioned prompt asset `engine/prompts/embedding.1.json`; `engine/app/vector_store.py` — `NullVectorStore` (no `ENGINE_DATABASE_URL`) + `PgsqlVectorStore` (asyncpg, `pgvector` halfvec, cached `dimension`/connection pool, `search_sessions` in `store_locale=local` mode) behind `get_vector_store()`; new `POST /api/v1/search/semantic` returning `{results, total, query_embedding, dimension}`; `ENGINE_DATABASE_URL` config; `asyncpg>=0.30` dep; sqlite-vec search tests re-patched onto the new provider/store seam. **Deviation (user-approved):** the architecture specified `sqlite-vec` for on-device ranking, but it is an alpha dependency that cannot run on this host — the app instead persists embeddings as **float32 blobs in drift (`embeddings` table, schema v10)** and ranks with **Dart cosine similarity** (`vector_codec.dart`), keeping all the semantics (query embedded engine-side, local-first retrieval, pgvector fills gaps) while swapping only the ranking engine. Flutter: `EmbeddingRepository` + `SemanticSearchRepository` (hybrid local-first merge, deduped by session id, sorted by similarity), `EmbeddingLocalDataSource` (drift blob upsert/read/delete + cosine ranking with threshold/exclude), `HybridSemanticSearchRepository`, `EngineGateway.semanticSearch`, `EngineSemanticSearch` parsing, `AnalysisController` persists the embedding stage output on every successful analysis (`job.intermediates.embedding`), session deletion cascades to embeddings, schema **v9 → v10** migration (`CREATE TABLE IF NOT EXISTS embeddings`), Search screen **Keyword / Semantic** SegmentedButton mode with similarity-percentage tiles. Tests: engine **222 pytest green, ruff clean** (7 new in `test_search.py`); Flutter **291 tests green + 3 skipped, `flutter analyze` clean** (migration v10 group + v9→v10 upgrade, `embedding_search_test.dart` codec/cosine/ranking/threshold/exclude/cascade + hybrid merge/degrade, semantic search-screen widget test). **Related-sessions suggestions** (surfacing top-similar sessions under each session) remain a follow-up with the graph browser §6.2.)*
- [x] Semantic search UI; embeddings backfill (Edge Function or worker). *(P6-A — Semantic search UI shipped in the Search screen (Keyword/Semantic mode). P6-B — Embeddings **backfill** completed: best-effort backfill controller and service, engine `/api/v1/search/embed_sessions` endpoint embeds existing sessions in batches of 10 and stores them in local float32 drift index, user-triggered via banner on Semantic search tab. Related-session suggestions live on session detail card using nearest-neighbor local cosine similarity.)*

### 6.2 Global knowledge graph
- [x] Global graph browse; node clustering by similarity; recommendations. *(P6-B — Global graph browse completed: "Knowledge map" screen displays all entities across the entire system ranked by session count, with interactive filter search; tapping an entity navigates straight to the per-session graph or opens a session picker when it is shared across multiple sessions.)*
- [x] Cross-session pattern detection (recurring projects, people, open tasks, repeated decisions — spec §24). *(P6-C — insights runner clusters entity types (person/project) and item types (task/decision) across sessions in addition to plain entities and tags; `kind` enum in `insights.schema.json` extended to `["entity","tag","person","project","task","decision"]`; engine Flutter-side `InsightKind` extended identically with per-kind icons; descriptors now ship entity `type` and item `type` so recurring projects/people/tasks/decisions surface as first-class insights.)*

### 6.3 Plugin system
- [x] `Plugin` adapter interface + OAuth2 credential store (server-side). *(P6-D — `engine/app/plugins/base.py` (`Plugin` protocol + `Credentials`/`PushReceipt` models + structured plugin exceptions), `engine/app/plugins/registry.py` (lazy plugin registry, server-side secret-store binding, OAuth2 pending-state store), `engine/app/routers/plugins.py` — `GET /api/v1/plugins`, `GET /plugins/{kind}/auth-url`, `POST /plugins/{kind}/token` (state-checked), `POST /plugins/{kind}/push` (draft validated against the `push_request` schema), `DELETE /plugins/{kind}/credentials`. Credentials never leave the engine.)*
- [x] First plugin targets (prioritized): Notion, Google Docs, Jira, GitHub, Slack, Todoist, Calendar (architecture §4.11). *(P6-D — Notion + Slack shipped first, exactly as spec §23 prioritizes: `engine/app/plugins/targets.py` — `NotionPlugin` (block transforms → `POST /v1/pages`) + `SlackPlugin` (markdown messages → `POST /chat.postMessage`), both OAuth2 with `httpx.MockTransport`-tested token exchange + push. `plugin.schema.json` (credentials/push_request/push_receipt) registered in `schemas.py` and vendored to `src/lib/data/contract/`.)*
- [x] Command → draft → plugin push flow. *(P6-D — `EngineGateway` + `EngineClient` gain `listPlugins`/`pluginAuthUrl`/`exchangePluginToken`/`pushDraft`/`disconnectPlugin`; `PluginsController` (`pluginsProvider`) manages connection state; Settings screen gains a **Plugins** section (connect → copy auth URL → paste code → connected; disconnect with confirm); the Draft screen AppBar gains a **Push to plugin** share menu that pushes the live-edited draft (`title`/`body`/`items`) to a connected target and shows the receipt link.)*

### 6.4 Universal input pipeline
- [x] Input adapters beyond voice: manual notes. *(P6-E — the `note` adapter is shipped: `engine/app/inputs/note.py` (`NoteInputSource.kind = "note"`, validates `meta["text"]`, yields a canonical `InputDoc` with `note: True` provenance, no `input_ref` needed) registered in `inputs/registry.py` (`supported: ["voice", "transcript", "note"]`) and exported from `inputs/__init__.py`; `orchestrator._ingest` treats `note` like `transcript` as a text-only kind. Flutter: `AnalysisController.analyzeNote(text, title)` submits `{kind: analyze, inputRef: null, options: {input_kind: note, input_meta: {text, title?}}}`; `StartNoteSession` usecase creates a session starting at `cleaning`; `NoteEditorScreen` (title + body, "Save & analyze") behind `/note/new`; home screen gains a **Write a note** entry point.)*
- [x] Input adapters beyond voice: images/PDFs (OCR), screenshots, emails/documents (parsers). *(P6-F images/PDFs via OCR, P6-G emails/documents via parsers, P6-H follow-up screenshots — see the four checked rows below.)*
- [x] Images/PDFs (OCR) — the OCR preprocessing seam. *(P6-F — engine gains an OCR stage before the shared pipeline, exactly like STT: `OcrProvider` protocol + `OcrResult(text, confidence)` in `inputs/base.py`, `PlaceholderOcrProvider` (`NOT_IMPLEMENTED` until a real OCR adapter lands in Phase 2), `ImageInputSource` (requires `mime_type.startswith("image/")`) and `PdfInputSource` (requires `application/pdf`), both delegating `extract_text(blob_ref)` and tagging meta with `mime_type`/`ocr_confidence`/`extracted: True`; providers registry gains `register_ocr`/`unregister_ocr`/`get_ocr` (default `ENGINE_DEFAULT_OCR` or `placeholder`), `inputs/registry.py` `supported: ["voice", "transcript", "note", "image", "pdf"]`, orchestrator passes `ocr_provider` from job options. Flutter: `DocumentPicker` seam (`file_picker`), `StartDocumentSession` (session at `uploading`, picks the file into the recordings dir), `AnalysisController.analyzeDocument(path, inputKind, mimeType)` submits `{kind: analyze, inputRef: <blob>, options: {input_kind: image|pdf, input_meta: {mime_type}}}`; `DocumentCaptureController` + a home **Import image/PDF** sheet entry. Verified: image and pdf jobs run the full cleanup→…→validation pipeline via fake OCR — `test_image_input_runs_pipeline_with_ocr`, `test_pdf_input_runs_pipeline_with_ocr`.)*
- [x] Emails/documents (parsers) — the parser preprocessing seam. *(P6-G — engine gains a parser stage before the shared pipeline, mirroring STT/OCR: `ParsedDocument` in `inputs/base.py`, `StdlibDocumentParser` (real, stdlib-only text extraction for `.txt`/`.md`/`.html`/`.rtf`/`.docx`/`.odt`/`.csv`/`.json`/`.xml`), `PlaceholderDocumentParserProvider` (`NOT_IMPLEMENTED` for unsupported mime types), `EmailInputSource` (title from `Subject`, body from text part or HTML-to-text, real `email`-package header decoding) and `DocumentInputSource` (both requiring a real parser, no blob→text loop); providers registry gains `register_parser`/`unregister_parser`/`get_parser` (default `ENGINE_DEFAULT_PARSER` or `placeholder`), `inputs/registry.py` `supported: ["voice", "transcript", "note", "image", "pdf", "email", "document"]`, orchestrator passes `parser_provider` from job options. Flutter: `DocumentPicker` grows `pickEmail`/`pickDocument`; the import sheet gains **Email file** and **Document** entries; `DocumentCaptureController` maps email → `message/rfc822` and documents via the engine's `DOCUMENT_MIME_TYPES` (unknown extensions rejected before a session is created) and submits `input_kind: email|document`. Verified: email and document jobs run the full cleanup→…→validation pipeline via fake parser — `test_email_input_runs_pipeline_with_parser`, `test_document_input_runs_pipeline_with_parser`.)*
- [x] Screenshots — rides the image/OCR path. *(P6-H follow-up — engine gains the **`screenshot` adapter** (#8): `ScreenshotInputSource` subclasses `ImageInputSource`, reusing its mime validation (`image/*`) and OCR delegation, and only adds provenance — `kind: "screenshot"`, meta `screenshot: True` — so downstream stages distinguish a screen capture from a photo with zero OCR specialization; registered in `inputs/registry.py` (`supported: ["voice", "transcript", "note", "image", "pdf", "email", "document", "screenshot"]`) and exported from `inputs/__init__.py`. The device-gated *capture* itself stays on the client; the engine ingests the resulting blob. Flutter: the import sheet gains a **Screenshot** entry (`Icons.screenshot_outlined`, "OCR-extract text from a screen capture"); `DocumentKind.screenshot` maps to `pickImage()`, the image mime table, and `input_kind: screenshot`. Verified: screenshot canonical-doc + mime-rejection + registry tests and full-pipeline `test_screenshot_input_runs_pipeline_with_ocr` with a registered fake OCR; Flutter capture-controller test asserts `input_kind: screenshot` / `image/png`. Engine suite **317 pytest green, ruff clean** (5 new). Flutter **339 tests green + 3 skipped, `flutter analyze` clean** (1 new). *Multi-input sessions remain deferred per spec ("if validated by usage"), not a code gap.*)*
- [ ] Ingest → `InputDoc` → shared orchestrator (architecture §4.12). No downstream changes expected — verify. *(P6-E verified for notes: a note job runs cleanup→…→validation with no STT and no input_ref — `test_note_input_runs_pipeline_without_stt`; P6-F verified for images/PDFs: an image and a pdf job run cleanup→…→validation with no downstream changes after the OCR preprocessing stage — `test_image_input_runs_pipeline_with_ocr` and `test_pdf_input_runs_pipeline_with_ocr`. Same contract already proven for `transcript`.)*
- [ ] Emails/documents ingest → shared orchestrator verified. *(P6-G verified: an email and a document job run cleanup→…→validation with no downstream changes after the parser preprocessing stage — `test_email_input_runs_pipeline_with_parser` and `test_document_input_runs_pipeline_with_parser`.)*
- [ ] Multi-input session support if validated by usage.

### 6.5 Testing
- [x] Embedding/retrieval quality tests; graph scalability smoke. *(P6-H — engine `tests/test_retrieval_quality.py`: a deterministic token-overlap embedding provider (dimension-pinned, L2-normalized, mirrors the on-device Dart `fakeEmbedding` family) drives the *real* ranking path — a 6-session corpus with query-related/distractor/other-user rows asserts related-over-unrelated ranking, top-k, threshold filtering, user scoping, the embedding stage's vector invariants (dimension, norm, exact composed text, empty-validation guard) and the `/search/semantic` endpoint preserving quality end-to-end. Flutter `test/data/retrieval_quality_test.dart`: a 23-session corpus (3 related, 20 distractors) asserts Precision@5 via `EmbeddingLocalDataSource.searchSimilar`. Engine `tests/test_graph_scale_smoke.py`: 1 000 entities × 5 000 relationships (aliases, duplicates, self-loops, dangling refs) through `assemble_canonical_session` — schema-valid, no dangling edges, deterministic ids, per-session edge ids, completes in <5 s. Engine suite **312 pytest green, ruff clean** (23 new).)*
- [x] Plugin adapter fixtures; OCR/PDF parsing goldens. *(P6-H — plugin HTTP responses extracted from inline handlers into recorded goldens `tests/fixtures/plugins/*.json` (Notion token/pages, Slack token/postMessage) served by `httpx.MockTransport`, plus two request-contract tests asserting the adapter's exact outgoing JSON against the fixtures. On-disk document goldens `tests/fixtures/documents/` (docx/odt/eml/rtf/html/md/txt/csv/json/xml + a hand-built xref-correct `sample.pdf`) parsed by `tests/test_parsing_goldens.py` with the production `StdlibDocumentParser` + `LocalBlobFetcher` asserting exact extracted text/title; the PDF golden also pins the OCR boundary (parser rejects `application/pdf`, `PlaceholderOcrProvider` fails `NOT_IMPLEMENTED` — the contract the Phase 2 OCR adapter validates against).)*

**Definition of Done:** semantic search + related sessions live, global graph browseable with recommendations, plugins push drafts to at least two targets, and voice-plus-image/PDF inputs flow through the same engine unchanged.

---

## Milestone log

*Append here after each milestone. Convention: update this file in the same change that completes the milestone.*

| Date | Milestone | Phase | Notes |
|---|---|---|---|
| — | Baseline approved | — | Architecture approved as binding baseline; roadmap created. No code yet. |
| 2026-08-06 | App scaffolding + local data layer | 1 | Flutter app in `src/` (Riverpod/go_router/drift/supabase). All 15 drift v1 tables + FTS5 + DAOs + repository interfaces. Domain use cases, engine HTTP client, auth/recording/settings/session-detail UI. `flutter analyze` clean; 19 unit/widget tests green on arm64 (drift via system libsqlite3, dio via fake adapter). Engine (`engine/`), RLS migrations, Docker, CI, outbox sync, e2e/release tooling pending. |
| 2026-08-06 | Engine skeleton | 1 | `engine/` FastAPI app: `/healthz`, `/readyz`, `/api/v1/jobs` (create/get/cancel/resume, ownership-scoped, envelope), SSE stream; rq worker entrypoint (Phase 1 jobs fail with structured `NOT_IMPLEMENTED` + resume); Supabase JWT middleware (JWKS, dev `X-User-Id` fallback); JSON Schema contract (`schemas/*.json`) vendored to `src/lib/data/contract/` with parity tests. Docker multi-stage image + compose (web/worker/redis) verified end-to-end (rq `job_id` kwarg collision, `ENV PYTHONPATH=/app` layout, envelope `exclude_none` fixes), GitHub Actions CI (flutter, engine, contract). |
| 2026-08-06 | Engine InputSource (voice adapter #1) | 1 | `engine/app/inputs/`: `InputSource` protocol + canonical `InputDoc`, `TranscriptionProvider` seam, `VoiceInputSource` adapter (mime/duration validation, metadata normalization), `PlaceholderTranscriptionProvider` (Phase 1 → `NOT_IMPLEMENTED`), registry `get_input_source("voice")`. 8 new pytest green (34 total), ruff clean, verified inside the Docker image. |
| 2026-08-06 | Outbox sync engine | 1 | drift schema v2 (`sync_outbox` + `sync_state`, v1→v2 migration) + `SyncDao`. Write-through outbox in `SessionLocalDataSource` (session mutation + outbox record in one transaction; canonical payload + `updated_at`). `SyncEngine`: oldest-first drain, bounded retry (`maxAttempts=5` → `failed`), incremental pull by `updated_at > cursor`, tombstones, no-loop guarantee (pull writes via DAOs only), tolerated pull failures. `SupabaseSyncRepository` full-tree push/pull/delete + cursor. `SyncController` auto-syncs on auth changes; sign-in triggers `syncNow()`. 19 new tests green (38 total), `flutter analyze` clean. |
| 2026-08-06 | Supabase RLS + local-stack validation | 1 | Committed `supabase/migrations/` (config.toml + initial schema): `sessions`/`topics`/`items` mirroring the PostgREST client contract (JSON-as-text columns, `timestamptz`, grants for `authenticated`/`service_role`), `user_id = auth.uid()::text` RLS with nested ownership for topics/items, storage bucket `sessions` + folder-scoped policies. Repo fixes: signed-out drafts push under the JWT owner (RLS enforces, client never trusts payload), tombstone scoped by id only, audio path `sessions/{user_id}/{session_id}.m4a`, `_sessionFromCloud` now round-trips confidence/transcript/audio/prompt fields. Validated against local Supabase stack: 15/15 curl checks (multi-user insert/select/tombstone + storage 403/404) + `test/data/supabase_integration_test.dart` (3 gated tests driving the real `SupabaseSyncRepository` against a live stack; skipped by default). |
| 2026-08-06 | Remote-stack validation (user's self-hosted Supabase) | 1 | Applied the same committed migration to the user's self-hosted Supabase at `132.145.181.76` (Kong `:8000`, supavisor poolers `:5432`/`6543` broken — `EAUTHQUERY`; applied via exposed pg-meta `POST /pg/query` as `postgres`; `sessions`/`topics`/`items` + `sessions` storage bucket verified via PostgREST + Storage API). Confirmed keys work for GoTrue health + admin API (confirmed-user provisioning). Integration test now supports stacks with email confirmations enabled via `SUPABASE_TEST_EMAIL`/`PASSWORD`(+`2`) dart-defines (sign-in path instead of signup); 3/3 green against the remote stack. `flutter analyze` clean; hermetic suite 38 pass + 3 skip. Managed project (cloud ref/URL) still not provided — validation was against the self-hosted server. |
| 2026-08-06 | Drift migration strategy + seed (schema v3) | 1 | Reworked the migration strategy to an explicit versioned chain: v1→v2 creates only `sync_outbox`/`sync_state`, v2→v3 adds `app_meta` (key/value) to track the seed version; removed the `createAll()` fallback in `onUpgrade`; `PRAGMA foreign_keys = ON` in `beforeOpen`. Seed v1 inserts the default AI provider rows for the signed-out `local` scope (STT: openai_whisper/deepgram/custom; LLM: openai/anthropic/ollama/custom) disabled-by-default, `INSERT OR IGNORE` + `app_meta` marker so it runs exactly once and never clobbers user edits. New `test/data/database_migration_test.dart` (4 tests): fresh-create (v3 + FTS + seed), seed idempotency across reopens, real v1→v3 upgrade (data + FTS preserved, sync tables + seed added), v2→v3 upgrade (outbox records preserved). Full suite now 42 green + 3 skipped; `flutter analyze` clean. |
| 2026-08-06 | Cloud convergence in session UI | 1 | §1.6 sync convergence is now user-visible. `SyncRunResult` gained `completedAt` + `copyWith`; `SyncController` stamps it per pass and guards against overlapping build-time + `syncNow` passes (single pass when a lazy resume read initializes the controller). Home: live sync status bar while signed in (Syncing… / last-run summary with pushed/pulled/deleted/failed / error) + pull-to-refresh driving `syncNow`. `SyncOnResume` (root `builder` widget) runs a sync pass on every app resume. Session detail re-reads the session when a sync pass completes, so cloud-landed content appears without navigating away. Tests (9 new): SyncController (stamps completedAt, syncNow re-runs, signed-out no-op), SyncOnResume (resume triggers exactly one pass per resume), home sync status/syncing/error/refresh + no-status-when-signed-out, session-detail convergence (cloud session renders after a sync pass). Full suite 51 green + 3 skipped (remote-stack integration re-run green); `flutter analyze` clean. |
| 2026-08-06 | Recording/settings/auth widget tests | 1 | §1.8 widget coverage completed for capture + provider config + auth. Added injectable seams to `RecordingController`: `voiceRecorderProvider` (swappable `VoiceRecorder`, production `RecordVoiceRecorder`) and `recordingOutputDirectoryProvider` (path_provider in prod, deterministic temp path in tests) so the flow is testable without platform plugins. New `test/helpers/recording_fakes.dart` (`FakeVoiceRecorder` + mutating in-memory repo). Tests (8 new): `RecordingController` unit tests (start→draft, pause/resume phase flips, cancel deletes draft, stop→ready with path/duration, permission-denial error), `recording_flow_test` widget tests (mic starts a draft + duration ticker 00:03 + stop marks ready via the home UI, permission denial stays idle with error), `settings_screen_test` widget tests (provider sections + Configured/Not-configured state, save persists model + API key into the secure-store seam, auth tile sign-in/out through the auth repository). Full suite 59 green + 3 skipped; `flutter analyze` clean. |
| 2026-08-06 | Fastlane + release tooling (scaffold) | 1 | §1.8 `--dart-define` env config was already live (`AppConfig`); added the release tooling scaffold. `src/Gemfile` + empty `Pluginfile`; `src/ios/fastlane/{Fastfile,Appfile}` — `staging` lane (unsigned `flutter build ios --no-codesign`) + `testflight` lane (signed build → `upload_to_testflight`, `APPLE_ID`/`TEAM_ID`); `src/android/fastlane/{Fastfile,Appfile}` — `staging` (debug APK) + `internal` lane (`flutter build appbundle --release` → Play internal track via `PLAY_JSON_KEY`); both lanes build the `--dart-define` flags from env (`ENGINE_BASE_URL`/`SUPABASE_URL`/`SUPABASE_ANON_KEY`) with a `require_defines!` guard. Android `build.gradle.kts` release signing now reads `ANDROID_KEYSTORE_*` env with a debug-key fallback (no keys committed). `src/.env.example` documents every var. **Verification blocked on this host** (no Xcode/Android SDK/Play credentials) — lanes must be exercised from CI or a Mac before the task can tick. |
| 2026-08-06 | Server-side provider secret store | 1 | §1.5 engine side done: provider keys live server-side (architecture §12). `app/secrets.py` — `SecretStore` protocol with `MemorySecretStore` (default/hermetic) + `RedisSecretStore` (compose topology; one hash per user), `normalize_provider`, and `resolve_provider_key` (user-configured secret first, then `{PROVIDER}_API_KEY` env for built-in providers). New `app/routers/providers.py` (`/api/v1/providers/{provider}/secret`): PUT upserts the user's key, GET returns only `{"configured": bool}` (never echoes the key), DELETE removes it — all user-scoped via the same `authenticate` dependency as jobs, with `INVALID_REQUEST` on blank keys / bad provider names (pydantic min-length 422 for missing key). Registered in `main.py`. Tests (9 new): round-trip + key never echoed, user-scoped isolation, delete, blank/invalid-input rejection, normalize, memory-store unit, env-fallback precedence. Engine suite now 43 pytest green, ruff clean. |
| 2026-08-06 | Pause/resume + live waveform | 1 | §1.4 capture is now fully interactive in the UI. `VoiceRecorder` gained `Stream<double> amplitude` (normalized 0..1); `RecordVoiceRecorder` subscribes to the `record` package's `onAmplitudeChanged` (120ms) and maps dBFS (−60..0) → 0..1. `RecordingState` carries a rolling `waveform` buffer (capped at 48 samples, cleared on stop/cancel) + `isPaused`/`isActive` getters; the controller wires/unwires the amplitude subscription around the recording lifecycle. Home `_RecordCard`: live waveform bars while active, pause/resume toggle button, paused label, and the stop control. Tests (2 new): controller (waveform accumulates in order, capped at capacity, cleared on stop), widget (waveform widget + pause/resume toggle through the home UI, waveform disappears on stop). Full suite 61 green + 3 skipped; `flutter analyze` clean. `just_audio` playback still lands with Phase 4 §4.3. |
| 2026-08-06 | P2-A: orchestration layer + prompt registry | 2 | **§2.1 + §2.3 + §2.2 protocols** — the AI Knowledge Engine pipeline is real. Prompt assets (`engine/prompts/<stage>.1.json` for all 7 stages) + `PromptRegistry` (immutable, version-mismatch guard, `resolve()` returns `{stage: version}`; `prompt_versions` recorded in the canonical session). `LLMProvider`/`TranscriptionProvider` Protocols + pluggable registry (placeholder providers default; `NO_PROVIDER` structured failure; lazy registration avoids the inputs↔providers import cycle). `StageConfig` (job `options["stage"]`/`options["stages"]` over engine defaults) + `TokenBudget` (chars/4 estimate → `TOKEN_BUDGET_EXCEEDED`). `StageContext` upgrades with `intermediates` + `require()`. Six `LLMStage` implementations (pydantic-validated outputs, e.g. entity/task/item type enums, confidence bounds) + deterministic `ValidationStage` that assembles the canonical Session→Topics→Items and pushes it through `session.schema.json` (`VALIDATION_FAILED` on breach). Malformed LLM output → exactly one corrective retry → `STAGE_OUTPUT_INVALID` (§2.1). Orchestrator: ingest via universal input pipeline (per-job `stt_provider` option), ordered run, per-stage persistence to `job.intermediates` + `stage` (resume-from-stage), idempotency (completed stages skipped), cancellation check between stages, `_run_pipeline` runs on a dedicated loop/thread so the sync worker also works under a running event loop. `job.schema.json` + Job model gained `intermediates` (contract re-vendored to Dart, Dart `Job` entity updated). Tests (27 new): prompt registry (immutability, version guard), stage config/token budget/retry semantics, full pipeline → schema-valid canonical session (titles, topics, items, prompt_versions), resume reruns only the failed stage, idempotent re-dispatch, cancel abort, budget stop, malformed-output retry + permanent failure. Engine suite **70 pytest green, ruff clean**; Flutter 61 green + 3 skipped, `flutter analyze` clean. STT/LLM provider adapters + golden fixtures remain (§2.2). |
| 2026-08-06 | P2-B: real STT/LLM provider adapters | 2 | **§2.2** — the placeholders are gone: real provider adapters, user-scoped keys, blob fetching, golden fixtures. LLM (`app/providers/llm.py`): `OpenAICompatibleLLM` (one class for OpenAI/OpenRouter/Ollama/LM Studio/custom `/chat/completions`, native `response_format` for structured output), `AnthropicMessagesLLM` (no native JSON mode → prompt-constrained JSON + new balanced-brace `extract_json`; the stage retry loop covers residual garbage), `GeminiLLM` (`responseMimeType: application/json`). STT (`app/providers/stt.py`): `OpenAIWhisperSTT`, `DeepgramSTT`, `AssemblyAISTT` (upload → submit → poll, status-aware). Keys: `app/secrets.py::resolve_provider_key` — user-configured secret first, then `{PROVIDER}_API_KEY` env; adapters read audio from the injected `BlobFetcher` (new `app/blobstore.py`: `LocalBlobFetcher` path-traversal-safe + `SupabaseBlobFetcher` REST; `ENGINE_BLOB_STORE=local|supabase`). Registry rewritten factory-based (`register_builtins()` at import; `get_llm`/`get_transcriber` thread `user_id`; registered instances still short-circuit so pipeline tests use fakes); `user_id` now flows through `get_input_source`/`build_stages`/orchestrator STT path. Config: `default_*_provider/model`, `provider_base_urls`, `provider_models`, `blob_store`/`blob_root`, env-overridable. Tests (19 new; `tests/test_providers.py` + `tests/fixtures/providers/*.json`): `extract_json` (fences/prose/braces-in-strings), golden `complete`/`complete_structured` per LLM family via `httpx.MockTransport` (no network), golden STT transcripts (Whisper/Deepgram; AssemblyAI upload→submit→poll + multi-poll + error), malformed/empty responses raise `ProviderOutputError`. Engine suite **89 pytest green, ruff clean**. |
| 2026-08-07 | P2-C: session lifecycle + SSE progress | 2 | **§2.4 (engine side)** — the lifecycle state machine (architecture §4.5) is first-class. New `app/lifecycle.py`: `SessionLifecycle` enum mirroring `session_status` in the canonical schema; `derive_session_status(job)` projects any job onto the machine (queued→uploading, running+no stage→transcribing, running+cleanup→cleaning, running+segmentation/classification/entity/task/knowledge→analyzing, running+validation→validating, succeeded→ready, failed/cancelled terminal; `None` for non-analyze kinds); `stage_label` (human-readable per-stage labels for the UI); idempotent `validate_transition`/`can_transition` DAG covering the whole machine incl. `ready→edited→synced`, failure/cancel from any pipeline state, and the resume path (`failed→uploading`). `job.schema.json` gained optional `session_status` (enum) + `stage_label`, re-vendored to `src/lib/data/contract/`; Dart `Job` entity parses both. SSE (`app/sse.py`) now emits `progress` events whenever the lifecycle projection changes (session_status + stage_label + job_status) plus typed terminal events — `done`/`failed`/`cancelled` (was a generic `done`) — and the jobs router enriches every job response via `job_payload`. Tests (36 new): lifecycle mapping matrix (parametrized), label coverage, payload enrichment (incl. non-analyze → no keys), transition legality/idempotency/terminal-stability/resume, SSE progress ordering through the full lifecycle, typed terminal events, non-analyze null-session progress, stage-change `job` events, plus API-level stream/terminal tests. Engine suite **125 pytest green, ruff clean**; Flutter 61 green + 3 skipped, `flutter analyze` clean. *Still open in §2.4: UI stage-progress/resume-render, session-detail v1 polish, transcript tab.* |

| 2026-08-07 | P2-D: prompt contract + cleanup goldens | 2 | **§2.3 bullet 3 + §2.5 bullet 2** — engine-side testing that pins the prompt/cleanup contract. Prompt pinning: `engine/prompts/*.json` mirrored byte-for-byte into `tests/fixtures/prompts/`; new `tests/test_prompt_contracts.py` (6 tests): shipped↔pinned byte parity + filename-set equality (in-place edits to a shipped prompt now fail loudly with a "bump the version" message), every `PIPELINE_STAGES` stage has a shipped prompt, asset filenames consistent with content (`name`/`version`/`json_schema_ref`), versions contiguous from 1, registry `latest`/`resolve` matches the highest pinned version, and every shipped asset round-trips `PromptAsset.model_dump()` identically (immutability/stable serialization). Cleanup goldens: new `tests/fixtures/cleanup/{meeting,technical,unclear}.json` (raw → cleaned pairs exercising filler removal, negation-heavy technical content, and `[unclear]` regions); new `tests/test_cleanup_goldens.py` (14 tests): invariant checks over every golden (numbers, negations, and capitalized names all preserved; filler words/phrases removed), the shipped cleanup prompt still carries the preservation/`[unclear]` constraints, and an end-to-end `CleanupStage` run against the pinned asset reproduces each golden (scripted provider, `original_text` echoed back). Engine suite **145 pytest green, ruff clean**. |

| 2026-08-07 | P2-E: SSE client + analysis progress UI | 2 | **§2.4 bullet 2** — the app now drives analysis and renders live engine progress. SSE client-side: `lib/data/engine/sse_parser.dart` (incremental frame decoder — buffered across network chunks, CRLF + comments + multi-line data, events default to `message`), and `EngineClient.streamJob` re-implemented over the real `/api/v1/jobs/{id}/stream` endpoint (was a 2s poll): parses `job`/`progress`/`done`/`failed`/`cancelled`/`heartbeat`, merges `progress` projections into the last job snapshot, yields the terminal job. `Job` gained `copyWith`; `SessionStatus.fromWire` added. New `features/analysis/analysis_controller.dart` (per-session `NotifierProvider.family`): `analyze()` submits `JobKind.analyze` with the recording as `input_ref` (local blob path for now; remote Supabase ref = follow-up), persists the job locally, projects `session_status` onto the local session (uploading→…→ready/failed/cancelled), applies the validated canonical result on success (`replaceTopics` + title/summary/prompt_versions/wordCount), surfaces the engine error message on failure, and guards duplicate submissions; `cancel()` + retry-by-resubmit. Session detail UI: `_AnalyzeCard` for unanalyzed drafts, `_ProgressCard` (stage label from `stage_label`, indeterminate bar, Cancel) while processing, `_FailureCard` (error + Retry); `sessionProvider` re-reads the session whenever analysis state changes so the applied result appears without navigating. Tests (16 new): SSE parser (framing, split chunks, CRLF, comments, defaults), engine-client streamJob (ordered job/progress/terminal + frames split across chunks via a streaming adapter), analysis controller (happy path applies canonical session, failure→retry resubmits, cancel lifecycle, no-audio fail-fast, duplicate-submit guard), session-detail widgets (Analyze → stage label → topics; failure → Retry). Full suite **77 green + 3 skipped**, `flutter analyze` clean. *Still open in §2.4: session-detail v1 polish, transcript tab, and post-stop auto-submit.* |

| 2026-08-07 | P2-F: transcripts + result wire fix | 2 | **§2.4 bullet 5** — the original vs cleaned transcript is now stored and readable. Fixed a latent wire bug first: `Job.fromJson` serialized nested engine payloads (`intermediates`/`result`/`error`) with Dart `Map.toString()` (e.g. `{cleanup: {cleaned_text: ...}}`), which `jsonDecode` cannot parse — this would have silently broken result application and error surfacing on the real engine path. It now `jsonEncode`s any non-string payload (string payloads pass through unchanged). Transcripts: on job success `AnalysisController` reads `intermediates['cleanup']['cleaned_text']`/`['original_text']` (§2.5 cleanup output) and persists them to `Session.originalTranscript`/`cleanedTranscript` (both for the `parsed` and fallback paths). Session detail gains `_TranscriptCard` — a Material 3 `SegmentedButton` Original/Cleaned toggle (default Cleaned) over a `SelectableText` body; renders whenever either transcript exists. Tests (4 new): `Job.fromJson` nested-map encoding + string passthrough + `copyWith` (new `test/domain/job_test.dart`), analysis controller persists both transcripts on success, and a transcript-card widget test (cleaned by default, toggles to original). Full suite **81 green + 3 skipped**, `flutter analyze` clean. *Still open in §2.4: session-detail v1 polish, post-stop auto-submit.* |

| 2026-08-07 | P2-G: session detail v1 polish | 2 | **§2.4 bullets 3 + 4** — the session-detail v1 screen is complete. Title renders from the canonical session (headline) with **alternative titles as chips** beneath it (from `session.alternative_titles`, produced by knowledge_extraction since P2-A); the one-paragraph summary shows under the title when present. Topic cards list items with the full type/priority/confidence hint set: type icon per `ItemType` (task/idea/decision/question/event/reminder/reference), a color-coded priority flag (`Icons.flag` red = high, `Icons.flag_outlined` orange = medium, `Icons.outlined_flag` grey = low, tooltips) and the low-confidence marker (`Icons.help_outline` + "Low confidence — verify this item" tooltip) for items with `confidence < 0.7`. Tests (1 new widget test): a ready session with alt titles + high-priority item + low-confidence item renders the chips, summary, priority flag, and help marker (and fixed a latent gap found by the test — `insertSession` never persists topics; topics only land via `updateSession`→`replaceTopics`, which the test now exercises). Full suite **82 green + 3 skipped**, `flutter analyze` clean. *Still open in §2.4: post-stop auto-submit (manual Analyze now).* |

| 2026-08-07 | P2-H: E2E (widget-level) + integration_test scaffold | 2 | **§2.5** — the record → engine mock → structured session flow now runs hermetically in CI. `test/features/e2e_record_to_session_test.dart` (2 widget tests) drives the real go_router routes with a real in-memory drift database, the `FakeVoiceRecorder`, and the controllable `FakeEngineGateway` as the engine mock: (1) mic → stop → session tile → detail → Analyze → SSE stage label ("Cleaning up") → succeeded job → title/alt-titles/summary/topics/priority flag render; (2) pre-seeded draft → Analyze → cleanup running → failed job → "boom" error + Retry → re-submit (createCount 2) → analyzing stage → succeeded → ready with topics. Landmarks: `HomeScreen` is the first test to watch a real drift `watchSessions` stream — flutter_test's pending-timer invariant tripped on drift's stream-close 0ms timer, so the tests unmount the tree inside the body (`pumpWidget(SizedBox())` + `pump(1ms)`) to flush it; the app-level `appRouter` singleton retains navigation state across tests, so each test builds a fresh `_router()`. On-device `integration_test/app_flow_test.dart` scaffolded (real mic + real engine; asserts SSE progress then ready-with-topics with a Retry-resume path) — **blocked on this host** (headless, no device/mic), mirrors Fastlane's scaffolded-unverified treatment. Full suite **84 green + 3 skipped**, `flutter analyze` clean. *Still open in §2.4: post-stop auto-submit.* |

| 2026-08-07 | P2-I: post-stop auto-submit | 2 | **§2.4** — recording stop now flows straight into analysis; no manual Analyze. `RecordingController.stop()` updates the draft to ready and fires the per-session `AnalysisController.analyze()` (unawaited — the controller is fail-safe: setup problems land as a retryable failure card, never a crash). Hardened `AnalysisController.analyze()`: the `authControllerProvider` read moved inside the guard so a missing auth override can't throw outside the try. Tests: new controller test (stop → exactly one analyze job with `input_ref == audioPath`, SSE subscribed, processing state) and the E2E tests now assert auto-submit (progress card + stage labels without any Analyze tap; go_router navigation advanced with fixed 400ms pumps because the indeterminate progress indicator makes `pumpAndSettle` time out, and `pump()` alone never finishes the page transition so Home's tile lingered in the tree). Recording flow tests unchanged — the fail-safe path means unoverridden engine gateways just yield a failed analysis state. Full suite **85 green + 3 skipped**, `flutter analyze` clean. *Phase 2 §2.4 fully done; §2.5 on-device E2E remains blocked (scaffold + hermetic equivalent live).* |

| 2026-08-07 | P3-A: edit ops + op-log (domain) | 3 | **§3.1 bullet 4 + §3.2 + §3.5 bullet 1 (domain foundation)** — the single mutation path (architecture §3.4) is first-class. `lib/domain/editing/edit_operations.dart`: sealed `EditOperation` with 15 subtypes covering every §3.1 edit (rename/add/delete/merge/split/reorder topics; insert/delete/move items; update text/type/priority/confidence; session title/summary), each a pure `apply(Session)` + exact `inverse()` + canonical `toJson`/`fromJson`. Invariants enforced by every apply: positions renumber as list indexes, no orphaned nodes. Design decisions: content edits (text/type) clear AI confidence and carry `oldConfidence` so undo restores it bit-for-bit; structural edits (move/merge/split/insert) *preserve* confidence; `SplitTopic` carries full moved-item snapshots (with pre-split positions) and `MergeTopics` places items at snapshot positions (clamped, order-exact inverse), so every op round-trips `apply → inverse.apply → bit-identical`. `lib/domain/editing/operation_log.dart`: cursor-based undo/redo with batch coalescing (`beginBatch`/`record`/`endBatch` for debounced drag/typing bursts), redo-tail trim on new edits, canonical JSON carrying the cursor so a persisted log restores with its undo position (§5.3 diff-emission shape). `lib/domain/repositories.dart` gains the `EditLogRepository` seam; `lib/domain/usecases/edit_session.dart` is the pure `EditSession` orchestration (apply/applyBatch/undo/redo → session + log persisted). Tests (13 new, `test/domain/editing_test.dart`): per-op behavior + invariants, full apply→inverse round-trip suite for all 15 ops, merge/split symmetry, undo/redo walk to bit-identical state, redo-tail trim, batch-coalesces-into-one-step, JSON+cursor round-trip, use-case persistence with in-memory repos. Bugs found and fixed by the tests: open-batch RangeError + missing redo-trim, `fromJson` losing the cursor, inverse ops not restoring confidence, and `SplitTopic.inverse()` dropping moved items (fixed via item snapshots + position-aware `MergeTopics`). Full suite **98 green + 3 skipped**, `flutter analyze` clean. *Next: P3-B — editing controller + drift persistence (session_oplog) + editing UI in session detail; P3-C version history.* |

| 2026-08-07 | P3-B: editing controller + drift op-log + editing UI | 3 | **§3.1 bullets 1–2 + §3.2 + §3.5 (UI + wiring)** — P3-A ops now drive a real editing UI. Drift **schema v4**: new `SessionOplog` table (`session_id` PK, `log_json`, `updated_at`), `database.dart` bumped to `schemaVersion => 4` with a `from < 4 → m.createTable(sessionOplog)` migration and a new `EditLogDao` (`get`/`save`/`remove`); `database.g.dart` regenerated via build_runner. `EditLogLocalDataSource implements EditLogRepository` (`getLog` → `OperationLog.fromJson(jsonDecode(row.logJson))`; `saveLog` → `insertOnConflictUpdate`), wired in `bootstrap.dart`. New `features/editing/editing_controller.dart`: per-session `EditingController extends FamilyNotifier` (`canUndo`/`canRedo`/`busy`/`revision`/`error`), `apply`/`undo`/`redo` delegate to `EditSession` (single mutation path persists session + op-log), `_loadLog()` restores the cursor on startup/after failure, `resetLog()` clears; `editingControllerProvider` + `editSessionProvider`. New `features/editing/editing_widgets.dart`: pure-UI dialogs/menus (title/summary/item text editors, topic editor with type dropdown, split check-list, merge/topic picker, priority + confidence dialogs). `session_detail_screen.dart` rewritten: `sessionProvider` watches the editing controller; ready sessions render `_EditToolbar` (undo/redo + Add topic), a `ReorderableListView` of `_TopicCard`s (rename/split/merge/up/down/delete, drag-reorder via `onReorderItem`), and `_ItemTile` menus (edit/move/priority/confidence/delete) + priority/confidence hints; non-ready sessions keep the read-only `Wrap` of cards; `_TopicCard` became a `ConsumerStatefulWidget` with working expand/collapse. Fixed alongside: `EditLogDao.delete` → `remove` (collided with `DatabaseConnectionUser.delete`), `showSplitDialog` returns `Future<List<String>?>`, async-gap `context` guards. Tests (10 new): 6 `editing_controller_test.dart` (apply persists session+op-log and bumps revision, undo/redo restore/re-apply, content-edit clears confidence and undo restores the original canonical JSON bit-exactly, op-log survives a restart via drift, no-op on missing session, resetLog) and 4 new widget tests in `session_detail_screen_test.dart` (toolbar undo/redo disabled before edits, edit-title cancel no-op, edit-title save → undo reverts, item edit via popup menu clears confidence → undo restores 0.9); `database_migration_test.dart` updated for schema v4 (fresh create + v1→v4 + v2→v4). Full suite **108 green + 3 skipped**, `flutter analyze` clean. *Next: P3-C version history (commit points on edit batches, list/diff UI, restore), P3-D diff-based sync (outbox op-log diffs + deterministic conflict resolver), P3-E remaining tests/docs.* |

| 2026-08-07 | P3-C: version history (commit points + diff + restore) | 3 | **§3.3 + §3.5 bullet 3** — every commit point in architecture §4.6 is live and every AI output is recoverable. Domain: `lib/domain/entities/session_version.dart` (`SessionVersion`: id/sessionId/versionNo/canonical snapshot/promptVersions/changeReason/createdAt); `lib/domain/versioning/session_diff.dart` — deterministic `diffSessions(before, after)` (10 `DiffKind`s: title/summary/topic-added-removed-renamed-moved/item-added-removed-moved-edited; stable order title→summary→added/removed→per-topic rename/move/add/remove/move/edit) + `summarizeDiff` (count-inclusive, e.g. "added 2 topics, edited 1 item") for change reasons. Seam: `VersionRepository` (`getVersions` oldest-first / `commit` / `getVersion`) + `versionRepositoryProvider`; `VersionLocalDataSource` (canonical-JSON snapshots; `commit` recomputes max+1 per session and self-assigns the id). Controller: `features/versioning/version_controller.dart` — `VersioningController` (async-mutex `_serialized`), `_init` loads + backfills **V1 "AI output"** when a ready session first opens with content; `commitEditBatch()` is driven from the session-detail widget `ref.listen` on `EditingState.appliedCount` (new counter — undo/redo bump `revision` but never `appliedCount`), so every committed edit batch lands as its own version with a `summarizeDiff` reason; `commitAnalysisResult()` snapshots prompt re-runs ("Re-analyzed with new prompts") and is called from `AnalysisController._onSucceeded`; `restore(n)` overlays the snapshot onto the live session (audio/transcripts/user fields preserved), writes "Restored from v{n}" as a new version, and clears the edit log so **undo can never cross the restore boundary** (restore-then-undo is a no-op). UI: `VersionHistoryScreen` (newest-first timeline, relative timestamps, current marker, selection toggles the per-version `_DiffPane` with kind icons/colors, bottom "Restore v{n}" bar + confirm dialog), opened from a session-detail AppBar history action (ready sessions only); `sessionProvider` now watches the versioning controller so commits/restores re-read the session. `bootstrap.dart` wires `versionRepositoryProvider`. Bugs fixed by tests: `commitEditBatch` infinite loop (`_lastApplied` advanced by a delta, not the absolute count) and `getVersions` returning newest-first (DAO DESC) against the ascending contract — now reversed in the data source. Tests (24 new; full suite **134 green + 3 skipped**, `flutter analyze` clean): 12 domain tests (`test/domain/versioning_test.dart` — diff ordering/determinism, rename-vs-move, item add/remove/move/edit, `summarizeDiff` count/plural/order); 10 controller tests (V1 commit, empty-session skip, per-batch versions with reasons, no-op guard, undo/redo-never-commit, restore + restore-then-undo, restart persistence, getVersion, re-run commit-point + no-op); 4 history-screen widget tests (newest-first, diff pane, initial-version note, restore flow). Hermetic analysis/detail/E2E widget tests got the `versionRepositoryProvider` override. *Phase 3 §3.3 done; §3.4 diff-based sync + §3.5 conflict resolver remain.* |

| 2026-08-07 | P3-D: diff-based sync (op-log outbox + conflict resolver) | 3 | **§3.4 + §3.5 bullet 4** — edits sync as **lossless diffs, not documents** (architecture §4.13). `lib/domain/editing/oplog_diff.dart`: `OplogDiff` (sessionId / base snapshot / ordered op batches / emittedAt), canonical `toJson`/`fromJson`. `lib/domain/editing/conflict_resolver.dart`: deterministic `ConflictResolver.resolve({remote, diff})` — replays each batch onto the *evolving merged* state, field-level LWW where the remote value is preserved in the flagged `FieldConflict` (equal values converge silently, sequential local edits never false-positive), structural ops (`DeleteTopic`/`MergeTopics`/`SplitTopic`/`MoveItem`/`AddTopic`/`ReorderTopic`) skip missing targets with a `StructuralConflict` or converge when the effect is idempotent, and positions **anchor-translate** onto the merged state (anchor = `base[position]` node id, translated to its merged index, clamped; reorder excludes its own node), so offline reorder-vs-reorder/split-onto-remote-gains/AddTopic-vs-remote-add interleave deterministically. `OperationLog` gains a monotonic `syncWatermark` (`unsyncedDelta()` emits the applied-but-unsynced tail — inverse ops when an undo rewound below the watermark; `markSynced()`), persisted as `sync_watermark` (backward-compatible `fromJson` defaults 0). Repositories: `SessionRepository.updateSession(session, {bool emitDiff})` — `emitDiff: true` enqueues an `oplog_diff` outbox record (base + ops read from the op-log at drain time), else the full-document `upsert`; new `ConflictRepository` (`addAll`/`forSession`/`clear`). `EditSession` apply/applyBatch/undo/redo now persist via `emitDiff: true` + capture the diff base on the clean→dirty transition (`EditLogLocalDataSource.saveLog`). `SyncEngine._pushDiff`: pull the cloud session (null → replay on the diff's own base), resolve, push the merged state, persist flagged conflicts to the new **schema v5 `sync_conflicts`** table, `markSynced` (the second edit anchors on the previously-merged state). Drift **schema v5**: `session_oplog.base_snapshot_json` + `sync_conflicts` table, `schemaVersion => 5`, versioned `onUpgrade` (`createTable` for <4 must skip the additive column — a real migration bug the v1→v5 test caught), `database.g.dart` regenerated. `SupabaseDataSource.pullSession` (single-session fetch incl. topics/items, deleted→null) + `NoopSyncRepository.pullSession`. Tests (24 new; full suite **159 green + 3 skipped**, `flutter analyze` clean): 15 resolver tests in `test/domain/conflict_resolver_test.dart` (two-device offline simulation: clean replay, LWW with remote-preserved, convergent edits, deleted-target skip/converge, split/merge/reorder/move/AddTopic anchoring); 4 oplog watermark tests (`editing_test.dart`: tail delta, markSynced, undo-below-watermark emits inverses, JSON round-trip incl. legacy default); 5 sync-engine diff-drain tests (clean diff push, never-synced resolves on own base, conflicting cloud edit → `conflicts: 1` + persisted `FieldConflict`, nothing-unsynced drains without pushing, second edit anchors on merged state); `database_migration_test.dart` → schema v5 (fresh create + v1→v5 + v2→v5 + v4→v5 with base column + table preservation). Two earlier resolver test expectations were wrong about op semantics (source items land at snapshot positions; AddTopic inserts *before* the anchored node) — fixed in the tests, resolver behavior was correct. *Next: P3-E — remaining tests/docs; Phase 3 §3.1 transcript re-run remains from the definition of done.* |
| 2026-08-08 | P3-F: transcript editing + re-run analysis | 3 | **§3.1 bullet 3 + spec §15** — the last open item in Phase 3's definition of done. Engine: new universal-input **`transcript` adapter** (`engine/app/inputs/transcript.py`, exported + registered in `engine/app/inputs/registry.py` and `__init__.py`) — `TranscriptInputSource.kind = "transcript"`, `ingest()` validates `meta["text"]` (non-empty, stripped, rejects with `InvalidRequestError` code `INVALID_REQUEST`), yields a canonical `InputDoc(kind="transcript", text, meta={source_ref, edited: True, language})` and needs **no `input_ref`**; `orchestrator._ingest` now allows `input_ref=None` when `input_kind == "transcript"` (all other kinds still raise `INPUT_INVALID`), so a transcript job runs cleanup→segmentation→…→validation while skipping STT; `_analyze_job` gained an `input_ref` param. Engine tests (151 green, ruff clean): transcript ingest canonical doc/trimming/missing-text rejection, registry voice/transcript/unknown, `test_transcript_input_runs_pipeline_without_stt` (full pipeline yields a canonical session and cleanup sees the transcript text), `test_voice_requires_input_ref`. Flutter: `UpdateSessionTranscript` op (`apply` → `copyWith(cleanedTranscript: newTranscript)`, exact `inverse`, JSON `old_transcript`/`new_transcript`, dispatch in `fromJson`) — **undoable, versioned, and diff-synced** like every other edit; `AnalysisController.analyzeTranscript(text)` guards processing + empty text, submits `{kind: analyze, inputRef: null, options: {input_kind: transcript, input_meta: {text}}}` (no upload/STT — project straight to `cleaning`), streams SSE to `ready`; `ConflictResolver` gains the field case (`session.cleaned_transcript`). UI (`session_detail_screen.dart`): `_TranscriptCard` is now a `ConsumerStatefulWidget` with **Edit** (multiline `showTranscriptEditor` dialog → `UpdateSessionTranscript`) and **Re-analyze** (`analyzeTranscript(cleaned)`) actions on ready sessions; `editing_widgets.dart` adds `showTranscriptEditor`. Test fakes: `FakeEngineGateway` captures `lastInputRef`/`lastOptions`. Tests (5 new; full suite **164 green + 3 skipped**, `flutter analyze` clean): transcript op apply/inverse/JSON round-trips, `analyzeTranscript` controller tests (transcript job shape + reaches ready; empty text fast-fail), transcript-edit + undo widget test, re-analyze widget test (transcript job, no input ref, Status: cleaning). *Phase 3 — Interactive Editing complete.* |
| 2026-08-08 | P4-A: global full-text search | 4 | **§4.1** — search is live end-to-end. Drift **schema v6**: new `SearchContent` table (`session_id` PK + aggregated `content`), external-content FTS5 `search_fts`, and **9 triggers** on `sessions`/`topics`/`items` that recompute the whole session's aggregate on any change (delete-from-index then `ON CONFLICT(session_id) DO UPDATE` — `INSERT OR REPLACE` inside a trigger fired by an upsert silently skips conflict resolution and `''` content in the FTS 'delete' command never matches, two real bugs the tests flushed out); v5→v6 migration creates the index and backfills existing sessions. `SearchDao`: `toFtsQuery` (whitespace tokenize → `"tok"*` AND-join, quoting neutralizes FTS syntax), `search` = bm25-ranked `customSelect` with `snippet(..., char(1), char(2), ' … ', 14)` markers, soft-delete exclusion. `SearchResult` domain entity + `SearchRepository` seam; `SearchLocalDataSource` maps raw `status` → `SessionStatus`. Search UI: `/search` route, home AppBar search icon, debounced (300ms) `SearchController`, highlighted title/snippet (`\x01`/`\x02` → bold spans), empty/loading/error states, tap → session detail. Cloud fallback (§5.4): `FallbackSearchRepository` (local-first, cloud on miss, cloud-down → empty not error), `NoopSearchRepository`, `SupabaseSearchDataSource` (RPC → `<mark>` → `\x01`/`\x02`), and `supabase/migrations/20260808000000_search_fts.sql` (`session_search` trigger-maintained table + GIN tsvector + SECURITY INVOKER `search_sessions` RPC with RLS + ownership policies) — **validated against Postgres 16** (both migrations apply, search/headline/delete-exclusion functional checks green). Fixed a latent bootstrap bug: the engine/auth/sync bindings were added to the overrides as bare providers, which Riverpod treats as self-overrides (no-ops) so those providers threw `UnimplementedError`; they now use `.overrideWith(...)` targeting the canonical providers, and the search binding follows suit. Full suite **182 green + 3 skipped**, `flutter analyze` clean; engine untouched (151 pytest green). *Open: §4.2 organization, §4.3 playback, §4.4 graph browser.* |
| 2026-08-09 | P4-B: organization (tags, favorites, archive, trash, pinning) | 4 | **§4.2** — organization is live end-to-end. Drift **schema v7** (`pinned` column + migration chain updates); new `TagsDao` + `TagLocalDataSource` (`tags`, `session_tags` tables; find-or-create case-insensitive; `attachByNames` for auto-tags; outbox enqueues for tag/session_tag). New `OrganizationController` (favorite/archive/pin toggles, trash=soft delete+clears archive, restore, purge=hard delete) + `TagsController` (tag list/stats/rename/delete/recolor). **Engine tags stage**: `engine/prompts/tags.1.json` asset + `TagExtractionStage` appended to the pipeline (cleanup→…→knowledge_extraction→tags→validation), emitting up to 20 `{name, confidence}` tags into the canonical session (schema carries `tags`); `AnalysisController._applyAutoTags` attaches them (additive, deduped, never overrides manual). Sync: 6 new `SyncRepository` methods, `SupabaseDataSource.pushTag/deleteTag/pushSessionTag/deleteSessionTag/pullTags/pullSessionTags`, `SyncEngine` drains tag/session_tag outbox records (colon-split composite id) + wholesale tag pull every pass. Cloud: `supabase/migrations/20260809000000_organization.sql` (`pinned` + `tags`/`session_tags` with RLS/cascade). UI: home `_FilterBar` tabs All/Favorites/Archived/Trash with per-tab empty states + pinned-first sort; session-detail AppBar star/archive/pin + trash popup + restore, tag chip row with color pick, `_MetadataRow` (date/duration/words/language). Tests (18 new): organization controller, tags controller, sync engine tag drain/pull/cascade, migration v7 on all upgrade paths, home filter tabs + pinned sort, session-detail org/metadata/tags widget. Full suite **204 green + 3 skipped**, `flutter analyze` clean. |
| 2026-08-09 | P4-C: audio playback + delete-audio-after-processing | 4 | **§4.3** — playback + privacy are live. New `SessionAudioPlayer` adapter (swappable, mirrors the `VoiceRecorder` pattern) with `JustAudioSessionAudioPlayer`: a lazy app-lifetime `just_audio` `AudioPlayer` (built on first `setSource`, `handleInterruptions` default), broadcasting `position`/`duration`/`playing`/`error` streams, `ProcessingState.completed` → not playing; background playback works because the player outlives screens (lock-screen media controls via `audio_service` tracked as a follow-up). App-scoped `PlaybackController` (`playbackControllerProvider`): play/toggle (same source resumes, different source reloads)/pause/seek/setSpeed/stop/stopForSession, loading + error states, one audible source at a time. Session-detail `_AudioCard` (local `audioPath` wins over cloud `audioRemoteUrl`): play/pause toggle, `0:00 / 0:30` readout, seek slider, 0.5–2.0× speed menu, delete-recording (confirm dialog → deletes the local file, stops playback, clears `audioPath`); editing the session keeps the audio (spec §16). **Privacy (spec §18, architecture §12)**: per-user "Delete audio after processing" `SwitchListTile` on Settings (`privacySettingsProvider` → `AppSettingsRepository` → `AppSettingsLocalDataSource` over the existing `app_meta` table, key `delete_audio_after_processing:<userId>`, signed-out → `local` scope); `AnalysisController._maybeDeleteAudioAfterProcessing` runs on success in both the parsed and transcripts-only paths and calls the `DeleteSessionAudio` use case (best-effort, never fails analysis); the privacy read goes straight to the repository (no provider-timing race). **Fixed a real drift bug**: DAO `updateSession` used `write(row)`, which treats null columns as "don't update", so clearing nullable fields (`audioPath`, `lastError`, …) silently never persisted — now `row.toCompanion(false)` writes every column including nulls; `clearError: true`/`clearAudioPath: true` now actually clear. Tests (23 new): playback controller (10), privacy controller (4), session-detail audio card widget tests (5, incl. delete-recording against a real temp file via `tester.runAsync`), analysis auto-delete (2), settings privacy toggle (1), `DeleteSessionAudio` (1). Full suite **227 green + 3 skipped**, `flutter analyze` clean. *Follow-up: `audio_service` lock-screen controls; on-device playback E2E blocked (no emulator/device on this host).* |
| 2026-08-09 | P4-D: knowledge graph browser | 4 | **§4.4** — the per-session graph is live end-to-end: engine extracts it, the app stores/serves/edits it (versioned + diff-synced like every other edit), and it survives sync. **Engine**: `engine/prompts/entity_extraction.2.json` (v2, pinned fixture added — fixtures immutable, never edited in place) emits `entities` + `relationships` into the canonical session; assembly enforces deterministic ids (`uuid5` over name for entities, per-session `uuid5(session_id, source_id, target_id, type)` for edges), alias casefold-dedupe, no-dangling-edges, and edge dedupe. **Flutter domain**: `EntityType`/`RelationType` with snake_case `wireName`/`fromWire`; `GraphEntity`/`GraphRelation`/`SessionGraph` models with canonical (contract) forms; `Session` carries `entities`/`relationships`; 8 graph `EditOperation`s (AddEntity, RenameEntity, MergeEntities, DeleteEntity, AddRelationship, RelabelRelationship, DeleteRelationship) all undoable/versioned via the op-log, with RestoreEntity/SplitEntity inverse handling added to `conflict_resolver.dart`; `SessionDiff` gains entity/relationship `DiffKind`s streaming into version-history summaries; **new `graph_ids.dart` replicates the engine uuid5 scheme with a self-contained SHA-1, verified byte-identical against Python**. **App data**: `GraphDao` per-session subgraph (`entitiesForSession`, `replaceSubgraph` transaction that upserts nodes, replaces membership + owned edges, prunes dangling edges + orphans), `traverse` (drift BFS, `maxDepth`-bounded), `deleteEntity`/`deleteRelation`; `GraphRepository` + `GraphLocalDataSource`; `SessionLocalDataSource` hydrates/persists the graph on read/update; sync pull persists it; analysis applies it on success; version restore includes it. **UI**: `GraphController` (all edits through the editing op-log) + `GraphScreen` (radial canvas + edge painter + relationship panel with confidence hints, node/relation bottom sheets, dialogs), entry via the session-detail app-bar graph icon. **Cloud**: `supabase/migrations/20260810000000_graph.sql` — `entities`/`session_entities`/`relationships` mirroring the drift tables with RLS + grants + indexes, plus the SECURITY INVOKER `graph_traverse(text, text, int)` recursive-CTE RPC (JSONB nodes+edges); `SupabaseSyncRepository` pushes the graph (entities upserted globally, membership + edges replaced per session so merge tombstones converge) and pulls it batched into `_sessionFromCloud`; gated PostgREST integration test extended to round-trip entities/relationships. **Validated against a real Postgres 16 container** (all four migrations apply; traversal returns nodes+edges with confidence/aliases; RLS isolation for a second user; soft-deleted edge excluded). Tests (13 new): `graph_ids_test` (uuid5 parity vs engine), `graph_controller_test` (6: persistence, casefold dedupe, rename clears confidence, merge rewires + drops self-loops, delete cascades, edge CRUD + undo/redo), `graph_screen_test` (5 widget tests). Full suite **240 green + 3 skipped**, `flutter analyze` clean; engine **164 pytest green, ruff clean**. *Remaining §4.4 item: global graph browse is Phase 6 — the first-class graph tables + drift/Postgres traversal already serve it.* |

| 2026-08-09 | P5-A: AI command bus | 5 | **§5.1** — the AI command bus is live end-to-end. **Engine**: 11 command prompt assets (`shorten_summary`, `expand_summary`, `rewrite_professional`, `rewrite_casual`, `extract_tasks`, `meeting_minutes`, `action_plan`, `email`, `report`, `presentation_outline`, `blog_post`) as versioned immutable assets (`engine/prompts/*.1.json`, pinned in `tests/fixtures/prompts/`) all referencing the shared `draft.schema.json` (canonical Draft shape: title/body/items with canonical item types). New `JobKind.command` + `engine/app/commands/runner.py` (`run_command` builds compact session context, renders prompt, `complete_structured_with_retry` → validated Draft; result `{command, session_id, prompt_versions, draft}`). Orchestrator `_run_pipeline` dispatches `JobKind.command`. Contract tests updated, new `test_commands.py` (9 tests: all commands produce valid drafts, context+params rendered, unknown command fails `UNKNOWN_COMMAND`, missing context `COMMAND_CONTEXT_INVALID`, invalid draft retry→`STAGE_OUTPUT_INVALID`, `process_job` dispatches command, API creates command job). Engine suite **181 pytest green, ruff clean**; `draft.schema.json` vendored to `src/lib/data/contract/`. **Flutter**: `JobKind.command` + `CommandDraft` entity; drift **schema v8** (`drafts` table + migration); `DraftRepository`/`DraftLocalDataSource` + `draftRepositoryProvider`; `command_catalog.dart` (11 commands, categories, icons, descriptions); per-session `CommandBusController` (builds context from session, submits `kind: command` job via `EngineGateway`, streams SSE, persists Draft on success); `DraftScreen` (editable title/body/items with type/priority/confidence; Save to session → `buildSaveDraftOperations` → `AddTopic` with items; Delete; auto-push on command success); command palette bottom sheet in session detail AppBar (visible when ready, grouped by category, disabled while running); `buildSaveDraftOperations` pure domain use case mapping body-only → reference item, structured items → typed items. Tests: `command_draft_test` (JSON round-trip + fromJobResult), `save_draft_to_session_test` (ops for body-only and structured drafts), `command_bus_controller_test` (submits correct options, persists draft), `draft_screen_test` (widget renders + save applies ops). Migration tests updated for schema v8. Full suite **240 green + 3 skipped**, `flutter analyze` clean. *Phase 5 §5.1 shipped; next §5.2 session chat.* |
| 2026-08-10 | P5-B: session chat | 5 | **§5.2** — per-session AI chat is live end-to-end: grounded answers over the canonical session JSON (topics/items/transcript/summary) with citations, confidence, and source links. **Engine**: new immutable prompt asset `engine/prompts/chat.1.json` (pinned byte-identical fixture in `tests/fixtures/prompts/` — fixtures immutable, never edited in place) requiring strict grounding in the given context with `[transcript]`/`[summary]`/`[topic: "…"]`/`[item: "…"]` citations, plus `engine/schemas/chat.schema.json` (canonical ChatResponse: answer/citations/confidence). New `JobKind.chat` + `engine/app/commands/chat_runner.py` (`run_chat` renders context via `render_chat_context`, validates question/context with `JobFailedError` codes `CHAT_QUESTION_INVALID`/`CHAT_CONTEXT_INVALID`, `complete_structured_with_retry` → pydantic `ChatResponse`); orchestrator `_run_pipeline` dispatches `JobKind.chat` (same SSE path as commands). Prompt-registry/contract tests updated (registry now `PIPELINE_STAGES | COMMAND_NAMES | {CHAT_PROMPT_NAME}`), new `test_chat.py` (runner validation, dispatch, API surface). Engine suite **181 pytest green, ruff clean**; `chat.schema.json` vendored to `src/lib/data/contract/` via `vendor_schemas.py`. **Flutter**: `ChatMessage` entity (`ChatRole.user/assistant`, toJson/fromJson mirroring `chat.schema.json`); drift **schema v9** (`chat_messages` table, `from < 9` migration, v8→v9 upgrade test, schema v9 checks in `database_migration_test.dart`); `ChatDao` (append, per-session stream, clear); `ChatLocalDataSource` + `chatRepositoryProvider` override in `bootstrap.dart`; `ChatController` (per-session `chatMessagesProvider`, submit via `EngineGateway` `kind: chat` SSE stream, persists user + assistant turns local-only like drafts — no sync/op-log path, `ChatPhase.idle/loading/error`, example questions); `ChatSheet` UI (example chips, composer, user/assistant bubbles with citations and source chips, `showModalBottomSheet` entry from session detail AppBar). New `test/features/chat_controller_test.dart` (5 tests) + `test/features/chat_sheet_test.dart` (2 tests; DB reads wrapped in `tester.runAsync`, `pump` not `pumpAndSettle` while the indeterminate answer spinner is visible). Flutter suite **248 tests green + 3 skipped, `flutter analyze` clean** (only pre-existing deprecation infos in `draft_screen.dart`).
| 2026-08-10 | P5-C: cross-session intelligence (insights) | 5 | **§5.3** — cross-session insights are live end-to-end, opt-in, with full provenance. **Engine**: new `engine/app/commands/insights_runner.py` — a **deterministic, provider-free** `run_insights(job)` (user decision: no LLM, no prompt asset for v1; vector similarity lands with Phase 6 §6.1 embeddings). It validates the shipped descriptors (`INSIGHTS_CONTEXT_INVALID`), clusters by **exact entity/tag label overlap** (casefold-dedupe, entities before tags), and emits templated statements — `"You've discussed {label} in {count} sessions."` / `"You've used the tag '{label}' in {count} sessions."` — with `min_sessions` (default 2), `max_insights` (20), deterministic confidence `round(min(0.95, 0.5 + (count-2)*0.15), 2)`, and `sources[{session_id,title,snippet}]` sorted by title (snippet search: items → summary → first 20k transcript chars, ~160-char window). New `engine/schemas/insights.schema.json` (+ `insights_schema()` accessor, `JobKind.insights`, vendored to `src/lib/data/contract/` via `vendor_schemas.py`, contract parity enforced), new `engine/app/routers/insights.py` `POST /api/v1/insights` (pydantic `InsightsSession`/`InsightsRequest`, user-scoped), orchestrator dispatches `JobKind.insights` → `run_insights` synchronously (no provider I/O). Engine suite **222 pytest green, ruff clean** (15 new tests in `test_insights.py`: clustering, casefold-dedupe, tags, confidence curve, max cap, provenance snippets, structural failures, dispatch, API surface). **Flutter**: `Insight`/`InsightSource`/`InsightKind`/`InsightResult` entities (toJson/fromJson mirroring `insights.schema.json`); `JobKind.insights`; `AppSettingsRepository.getEnableInsights/setEnableInsights` over `app_meta` key `enable_cross_session_insights` (off by default); `IntelligenceSettings` provider; `InsightsController` (`sessionToInsightDescriptor` ships only compact descriptors — title/summary/items/tags/entities + transcript capped at 4000 chars; only analyzed sessions `ready`/`edited`/`synced`; SSE stream to `succeeded`); `InsightsScreen` (opt-in gate, running/error/empty states, insight cards with session-count + confidence chips and **source chips → open the source session**, Regenerate); router `/insights` route; home AppBar insights icon; Settings "Enable cross-session insights" toggle. Widget-test bugs found and fixed: the opt-in gate overflowed short viewports (now `SingleChildScrollView`) and the auto-refresh fired before the async settings provider resolved (now awaits `intelligenceSettingsProvider.future`). New tests (14): `insight_test.dart` (4 JSON round-trips/malformed), `insights_controller_test.dart` (7: descriptor shape/caps, analyzed filter, empty-library stays idle with no job, happy path, structured error), `insights_screen_test.dart` (3 widget tests: opt-in gate computes nothing, pass renders statements + provenance + navigation, empty state). Flutter suite **262 tests green + 3 skipped, `flutter analyze` clean**. *§5.4 AI memory and §5.5 export pipeline remain.* |


| 2026-08-11 | P6-A: semantic search | 6 | **§6.1** — semantic search is live end-to-end, local-first. **Engine**: pipeline grows a stage-8 `embedding` stage (sentence-transformers `all-MiniLM-L6-v2`, 384-dim) with versioned prompt asset `engine/prompts/embedding.1.json`; `engine/app/vector_store.py` — `NullVectorStore` (no `ENGINE_DATABASE_URL`) + `PgsqlVectorStore` (asyncpg + `pgvector` halfvec with cached dimension/pool, `search_sessions(..., store_locale="local")`) behind a `get_vector_store()` seam; new `engine/app/routers/search.py` `POST /api/v1/search/semantic` → `{results, total, query_embedding, dimension}` (user-scoped); `ENGINE_DATABASE_URL` config; `asyncpg>=0.30`; sqlite-vec tests re-patched onto the provider/store seam. **Approved deviation**: architecture §5.3 called for `sqlite-vec` for on-device ranking, but it is an alpha dependency that cannot run on this host — the app persists embeddings as **float32 blobs in a new drift `embeddings` table (schema v10, guarded `CREATE TABLE IF NOT EXISTS` migration)** and ranks with **pure-Dart cosine similarity** (`vector_codec.dart`); pgvector cloud ranking + engine-embedded queries keep the design identical, only the on-device ranking engine changed. **Flutter**: schema **v9 → v10**; `EmbeddingRepository` (upsert/read/delete) + `SemanticSearchRepository` (hybrid local-first merge, dedupe, sort) + `EmbeddingLocalDataSource` (blob codec, cosine ranking, threshold/exclude) + `HybridSemanticSearchRepository` + `EngineGateway.semanticSearch`; `AnalysisController` persists the `embedding` stage output on every successful analysis (`job.intermediates.embedding`), session delete cascades to embeddings; Search screen **Keyword / Semantic** SegmentedButton mode with similarity-percentage tiles. **Tests**: engine **222 pytest green, ruff clean** (7 new in `test_search.py`); Flutter **291 tests green + 3 skipped, `flutter analyze` clean** (migration v10 + v9→v10 upgrade, new `embedding_search_test.dart` — codec/cosine/round-trip/ranking/threshold/exclude/cascade + hybrid merge/degrade/fallback, semantic-mode search-screen widget test). **Drive-by fixes**: the half-shipped §5.5 export feature (WIP `session_exporter.dart`/`export_controller.dart`/`export_bottom_sheet.dart` from the pre-commit tree) had compile errors (`ExportFormat` misplaced, missing `dart:convert`, stale exact-match test expectations on the `memory` options key) — fixed, path_provider mocked in `export_test.dart`, 4 export tests green. *Follow-ups: §6.1 related-sessions suggestions, embeddings backfill for pre-existing sessions, §5.4 AI memory, §5.5 export router/wiring.* |
| 2026-08-11 | P5-F: §5.6 testing gap (drafts never auto-applied + memory provenance) | 5 | **§5.6** — tests only, no new user-facing feature. **Command draft invariant** (architecture §4.11, spec §23: drafts are never auto-applied): new `src/test/features/command_bus_controller_test.dart` (3 tests) drives `CommandBusController` through the real drift stack + `FakeEngineGateway` — a succeeded `meeting_minutes` job persists exactly one editable `CommandDraft` (command/title/body/items/`prompt_versions` provenance) and **leaves the session bit-identical** (no new topic/item/title change); a failed job produces no draft and leaves the session untouched; and a draft enters the session **only** through the explicit user save path (`buildSaveDraftOperations` → `AddTopic` → editing op-log apply → draft delete — the exact Draft-screen flow). **Memory provenance** (architecture §4.9: every memory-backed answer stays traceable): `chat_controller_test.dart` gains a 3-test memory group — with AI memory on and a related session present, the chat job ships a source-tagged descriptor (`session_id`/title/summary/open_tasks), `ChatState.memorySources` surfaces what rode into the prompt, and the persisted assistant answer keeps its `[memory: "…"]` citation; memory ships empty when AI memory is off (default) or the session opts out. `memory_context_test.dart` gains the invariant that every descriptor in a built memory block carries a non-empty source `session_id`. **Engine-side provenance contract**: `test_memory.py` pins the chat prompt asset that *generates* the answers — it must mandate inline citations (`citing sources inline like … [memory: "Title"]`, `never hallucinate`), closing the loop on "every answer cites sources" at the prompt layer. Counts: Flutter **299 tests green + 3 skipped, `flutter analyze` clean** (only the 2 pre-existing `draft_screen.dart` deprecation infos); engine **223 pytest green, ruff clean** (1 new in `test_memory.py`). |
| 2026-08-11 | P6-B: §6.1 embeddings backfill + §6.2 global graph browse | 6 | **§6.1 completions + §6.2 part 1** — user-triggered embeddings backfill, related-sessions recommendations, and global graph browsing are live end-to-end. **Engine**: new `/api/v1/search/embed_sessions` endpoint embeds existing raw text payloads in batches (capped at 50) and returns vectors + dimensions. Engine suite **228 pytest green, ruff clean** (5 new tests in `test_search.py`). **Flutter**: `embedSessions` added to `EngineGateway` and implemented across `EngineClient`/fakes; `EmbeddingRepository.sessionsWithoutLocalEmbedding()` identifies analyzed sessions missing a `local` embedding; debounced `EmbeddingBackfillController` manages best-effort backfill (chunks of 10) and upserts vectors; Search screen Semantic tab shows a top banner with an "Embed" action when pre-embedding sessions are detected. `relatedSessionsProvider` retrieves a session's on-device neighbors with `similarity >= 0.7` and displays them in a `_RelatedSessionsCard` recommendation widget on the session detail screen. `GraphRepository.sessionIdsForEntity` lets `GlobalGraphController` load all system entities, match their owning sessions, and sort them descending by occurrence; "Knowledge map" screen lists entities with an interactive filter, tapping a shared entity prompts a modal sheet picker to open its per-session graph route `/graph/:id`. Home screen AppBar gains a "Knowledge map" button for global graph browse. Counts: Flutter **306 tests green + 3 skipped, `flutter analyze` clean** (7 new tests across `embedding_backfill_controller_test.dart`, `related_sessions_controller_test.dart`, and `global_graph_controller_test.dart`). |
| 2026-08-12 | P6-C: §6.2 cross-session pattern detection | 6 | **§6.2 complete** — recurring projects, people, open tasks, and repeated decisions now surface as first-class insights. **Engine**: `insights_runner.py` `_clean_label` accepts entity/item dicts (`name`/`title`/`label`), new `_determine_kind` maps an entity's `type` to `person`/`project` (and item `type` to `task`/`decision`), `_statement` gains per-kind phrasing, and `_build_insights` clusters three axes — entities (type-aware), tags, and task/decision items — so the deterministic, provider-free `POST /api/v1/insights` pipeline detects cross-session patterns without any new endpoint or LLM. `insights.schema.json` `kind` enum extended to `["entity","tag","person","project","task","decision"]` and vendored to Flutter. Counts: engine **229 pytest green, ruff clean** (1 new `test_pattern_detection_person_project_task_decision`); **Flutter**: `InsightKind` enum extended identically, `sessionToInsightDescriptor` now ships entity `type` + item `type` in the descriptor, `_InsightCard` renders per-kind icons (`person_outline`/`assignment_outlined`/`check_circle_outline`/`gavel_outlined`), descriptor-shape tests updated — **306 tests green + 3 skipped, `flutter analyze` clean**. |
| 2026-08-12 | P6-D: §6.3 plugin system | 6 | **§6.3 complete** — command drafts can now be pushed to real outbound targets, end-to-end between the engine and the app. **Engine** (`engine/app/plugins/`): `base.py` (`Plugin` protocol: `authorization_url`/`exchange_token`/`push`, `Credentials`/`PushReceipt` models, structured `PluginNotConfiguredError`/`PluginNotConnectedError`/`OAuthStateInvalidError`/`InvalidDraftError`); `targets.py` — `NotionPlugin` (token exchange + block-transform draft → `POST /v1/pages`, returns page URL) and `SlackPlugin` (markdown-format draft → `POST /chat.postMessage`, returns channel/message link); `registry.py` (lazy global registry, `load_credentials`/`save_credentials`/`delete_credentials` bound to the server-side secret store, `save_pending_state`/`pop_pending_state` OAuth2 state store). `app/routers/plugins.py`: `GET /api/v1/plugins` (per-user connection status list), `GET /plugins/{kind}/auth-url` (state + URL), `POST /plugins/{kind}/token` (state-checked exchange, credentials persisted server-side), `POST /plugins/{kind}/push` (draft validated against the `plugin.schema.json` `push_request` definition → `InvalidDraftError`), `DELETE /plugins/{kind}/credentials`. New shared `engine/schemas/plugin.schema.json` registered in `schemas.py` + vendored to `src/lib/data/contract/` (contract parity). OAuth + pushes tested hermetically via `httpx.MockTransport` (no network). Engine suite **248 pytest green, ruff clean** (19 new in `test_plugins.py`). **Flutter**: new `PluginTargetStatus`/`PluginAuthUrl`/`PluginPushReceipt` entities (`domain/entities/plugin.dart`); `EngineGateway` + `EngineClient` + all fakes gain `listPlugins`/`pluginAuthUrl`/`exchangePluginToken`/`pushDraft`/`disconnectPlugin`; `PluginsController` (`pluginsProvider`, out-of-band OAuth — the app opens the auth URL in a browser and the user pastes the code back; redirect `ai-knowledge-companion://oauth/callback`); Settings screen **Plugins** section (connect/disconnect with confirm, connection status, engine-missing states handled); Draft screen **Push to plugin** share menu pushing the live-edited draft and showing the receipt link. Counts: Flutter **321 tests green + 3 skipped, `flutter analyze` clean** (15 new: 6 controller, 3 section widget, 1 draft-screen push, 5 engine-client plugin endpoints). |
| 2026-08-13 | P6-E: §6.4 universal input — manual notes | 6 | **§6.4 part 1** — non-voice input is live: manual notes flow through the *same* orchestrator with no STT, no blob, and no downstream changes. **Engine**: new universal-input **`note` adapter** (`engine/app/inputs/note.py` — `NoteInputSource.kind = "note"`, validates non-empty `meta["text"]`, yields a canonical `InputDoc(kind="note", text, meta={source_ref, note: True, title?, language})`, no `input_ref` needed), exported + registered in `inputs/registry.py` (`supported: ["voice", "transcript", "note"]`); `orchestrator._ingest` now treats `note` like `transcript` as a text-only kind (still `INPUT_INVALID` for blob kinds with a null ref). Engine suite **254 pytest green, ruff clean** (6 new: note ingest canonical/trimming/title-omission/missing-text, registry resolves note + supported list, `test_note_input_runs_pipeline_without_stt` full-pipeline run). **Flutter**: `AnalysisController.analyzeNote(text, {title})` — guards processing + empty text, submits `{kind: analyze, inputRef: null, options: {input_kind: note, input_meta: {text, title?}}}` (projects straight to `cleaning`), streams SSE to `ready`; `StartNoteSession` usecase creates a session starting at `cleaning` with the note text kept as `originalTranscript`; new `NoteEditorScreen` (`/note/new`: optional title + body, **Save & analyze** → creates session → kicks analysis → lands on the session detail screen); home screen gains a **Write a note** entry point. Counts: Flutter **327 tests green + 3 skipped, `flutter analyze` clean** (6 new: 3 controller `analyzeNote` tests, 2 note-editor widget tests, 1 home entry test; 2 existing home tests switched to a tall viewport since the new button pushes lazy-list tiles below the fold). |
| 2026-08-13 | P6-F: §6.4 universal input — images/PDFs via OCR | 6 | **§6.4 part 2** — non-voice *blob* input is live: images and PDFs run the **same** orchestrator unchanged after an OCR preprocessing stage (mirrors STT exactly). **Engine**: OCR seam in `inputs/base.py` (`OcrProvider.extract_text(blob_ref, *, mime_type=None) -> OcrResult(text, confidence 0..1)`), `PlaceholderOcrProvider` (`NOT_IMPLEMENTED` until a real OCR adapter lands in Phase 2), new `ImageInputSource` (requires `mime_type.startswith("image/")`) and `PdfInputSource` (requires `application/pdf`) both delegating to OCR and tagging meta with `mime_type`/`ocr_confidence`/`extracted: True`; providers registry gains `register_ocr`/`unregister_ocr`/`get_ocr` (default `ENGINE_DEFAULT_OCR` env or `placeholder`) alongside the STT factories; `inputs/registry.py` `supported: ["voice", "transcript", "note", "image", "pdf"]` and orchestrator passes `ocr_provider` from job options. Engine suite **265 pytest green, ruff clean** (11 new: image/pdf canonical docs + mime rejections, placeholder NOT_IMPLEMENTED, OcrResult confidence bounds, registry image/pdf resolution, and full-pipeline `test_image_input_runs_pipeline_with_ocr` + `test_pdf_input_runs_pipeline_with_ocr` with a registered fake OCR). **Flutter**: `DocumentPicker` seam (`pickImage`/`pickPdf` via `file_picker` — the swappable test hook), `StartDocumentSession` usecase (session at `uploading` with the picked path), `AnalysisController.analyzeDocument(path, {inputKind, mimeType})` submitting `{kind: analyze, inputRef: <blob>, options: {input_kind: image|pdf, input_meta: {mime_type}}}` and streaming SSE to `ready`; `DocumentCaptureController` (pick → copy into the recordings dir → create session → analyze → navigate) + a home **Import image/PDF** bottom sheet entry. Counts: Flutter **333 tests green + 3 skipped, `flutter analyze` clean** (6 new: 2 `analyzeDocument` controller tests, 3 document-capture controller tests, 1 home import-entry test). |
| 2026-08-13 | P6-G: §6.4 universal input — email/document parsers | 6 | **§6.4 part 3** — parser-based input is live: emails and office/text documents run the **same** orchestrator unchanged after a parser preprocessing stage (mirrors STT/OCR). **Engine**: `ParsedDocument(title, text, meta)` + `DocumentParser.parse(blob_ref, *, mime_type) -> ParsedDocument` seam in `inputs/base.py`; `StdlibDocumentParser` (real, stdlib-only extraction: text passthrough, HTML-to-text via `html.parser`, RTF via `re`-based tag stripping, `.docx` zip+XML w+text() concat, ODF `content.xml` w+text()); `PlaceholderDocumentParserProvider` (`NOT_IMPLEMENTED` — real adapters configured via `ENGINE_DEFAULT_PARSER`); new `EmailInputSource` (title from `Subject` with RFC 2047 header decoding via `email`/`header` stdlib, body from text part or HTML-to-text fallback) and `DocumentInputSource`, both raising `INPUT_INVALID` on unknown mime types and mapping parser failures to `ProviderOutputError`; providers registry gains `register_parser`/`unregister_parser`/`get_parser` + `_default_parser_name`; `inputs/registry.py` `supported: ["voice", "transcript", "note", "image", "pdf", "email", "document"]`; orchestrator passes `parser_provider` from job options. Engine suite **289 pytest green, ruff clean** (24 new: email/document canonical docs + mime routing + rejections, placeholder NOT_IMPLEMENTED, registry resolution, 12 real-parser tests covering txt/md/html/eml/rtf/docx/odt/csv/json/xml + corrupt-zip/missing-blob/unsupported, and full-pipeline `test_email_input_runs_pipeline_with_parser` + `test_document_input_runs_pipeline_with_parser` with a registered fake parser). **Flutter**: `DocumentPicker` grows `pickEmail`/`pickDocument`; the import sheet gains **Email file** and **Document** entries; `DocumentCaptureController` maps email → `message/rfc822` and documents via the engine's `DOCUMENT_MIME_TYPES` mirror (unknown extensions rejected before a session is created) and submits `input_kind: email|document`; `AnalysisController.analyzeDocument` already handles any kind. Counts: Flutter **336 tests green + 3 skipped, `flutter analyze` clean** (3 new capture-controller tests: email, document, unsupported-extension). |
| 2026-08-13 | P6-H: §6.5 quality testing | 6 | **§6.5 complete** — quality tests for the retrieval and graph subsystems, plus fixture-driven plugin/document goldens. **Embedding/retrieval quality** (`engine/tests/test_retrieval_quality.py`, 8 new): a deterministic token-overlap `EmbeddingProvider` + `MemoryVectorStore` (protocol-compatible pgvector stand-in) assert the real ranking path on a 6-session corpus — related-over-unrelated ordering, top-k, threshold filtering, user scoping, `EmbeddingStage` vector invariants (dimension, L2 norm, exact composed text, empty-validation guard) and `/search/semantic` preserving quality end-to-end. Flutter `src/test/data/retrieval_quality_test.dart` (2 new): a 23-session corpus (3 related, 20 distractors) asserts Precision@5 + descending-similarity ordering through `EmbeddingLocalDataSource.searchSimilar` with a token-overlap embedding mirroring the engine harness. **Graph scalability smoke** (`tests/test_graph_scale_smoke.py`, 2 new): 1 000 entities × 5 000 relationships (aliases, duplicates, self-loops, dangling refs) through `assemble_canonical_session` — schema-valid, no dangling edges, deterministic entity ids + per-session edge ids, completes in <5 s. **Plugin adapter fixtures**: the Notion/Slack HTTP responses moved from inline handlers into recorded goldens `tests/fixtures/plugins/*.json` (served via `httpx.MockTransport`), plus 2 new request-contract tests asserting the adapter's exact outgoing JSON matches the fixtures (Notion page blocks + Slack markdown payload). **OCR/PDF parsing goldens**: 11 on-disk fixtures in `tests/fixtures/documents/` (docx/odt/eml/rtf/html/md/txt/csv/json/xml + a hand-built xref-correct `sample.pdf`) parsed by `tests/test_parsing_goldens.py` (11 new) with the production `StdlibDocumentParser` + `LocalBlobFetcher` asserting exact extracted text/title; the PDF golden also pins the OCR boundary (parser rejects `application/pdf`, `PlaceholderOcrProvider` `NOT_IMPLEMENTED` — the contract the Phase 2 OCR adapter validates against). Engine suite **312 pytest green, ruff clean** (23 new). Flutter **338 tests green + 3 skipped, `flutter analyze` clean** (2 new). *Remaining in §6.4: screenshots (device-gated capture, rides the image/OCR path) and multi-input sessions (deferred: "if validated by usage"). Real OCR adapters remain Phase 2.* |
| 2026-08-13 | P6-H follow-up: §6.4 screenshots input | 6 | **§6.4 screenshots shipped** — the last §6.4 code item is closed out. **Engine**: `engine/app/inputs/screenshot.py` — `ScreenshotInputSource` (adapter #8) subclasses `ImageInputSource`, reusing its `image/*` mime validation and OCR delegation verbatim, and only adds provenance (`kind: "screenshot"`, meta `screenshot: True`); registered in `inputs/registry.py` (`supported` list gains `"screenshot"`, resolved via the same `_document_source` OCR path) and exported from `inputs/__init__.py`. No downstream stage changes — screenshots run the shared pipeline exactly like images. Engine suite **317 pytest green, ruff clean** (5 new: registry resolution, canonical-doc provenance, mime pass-through, non-image rejection, full-pipeline `test_screenshot_input_runs_pipeline_with_ocr`). **Flutter**: import sheet gains a **Screenshot** entry; `DocumentKind.screenshot` → `pickImage()`, the image mime table, and `input_kind: screenshot`. Counts: Flutter **339 tests green + 3 skipped, `flutter analyze` clean** (1 new capture-controller test). *Remaining in §6.4: multi-input sessions only — deferred per spec ("if validated by usage"), not a code gap. Real OCR adapters remain Phase 2.* |
---

## Cross-cutting checklist (applies to every phase)

- [ ] Every PR passes CI: analyze/lint → unit → widget → golden/prompt tests → contract parity → e2e smoke (§10.3).
- [ ] Canonical JSON changes go through `engine/schemas/*.json` + vendored Dart copy + contract test (§5.2).
- [ ] New AI behavior is a prompt asset or a stage — never inline prompt strings (§4.3, §4.2).
- [ ] New provider = adapter + golden fixtures, no pipeline changes (§4.4).
- [ ] New output surfaces as an editable Draft or editable content — AI never locks anything (§1.2).
- [ ] Roadmap.md updated in the same change as the milestone it records.
